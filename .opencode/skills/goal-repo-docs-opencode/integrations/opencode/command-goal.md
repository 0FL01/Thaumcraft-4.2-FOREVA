---
description: Start or resume a compact durable goal for long OpenCode work
---

Load the `goal-repo-docs` skill.

If `.opencode/goal.md` exists, rehydrate it against live Git before editing. Do not merge a different objective into an active anchor; archive/replace a completed one, or replace an active one only when the user explicitly changed the objective. Otherwise create the anchor for the objective below. Work only toward explicit Done criteria, keep one Now and one Next action, update the anchor at material boundaries, and stop when Done passes.

Objective:

$ARGUMENTS
