## Durable long-task anchor

When `.opencode/goal.md` exists, load `goal-repo-docs` and reconcile that file with live Git before substantive edits, especially after Compact/DCP or a session/model switch. Keep the anchor concise, update it only at material boundaries, and never treat `Deferred` items as current scope.
