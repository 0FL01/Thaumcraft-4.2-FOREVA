# Durable Goal Progress

Last updated: 2026-05-16
Branch: `codex/durable-goal-stage8-9`

> Condensed with user approval. The verbose checkpoint prose was produced by another agent and moved to `docs/GOAL_PROGRESS.archive.md`.

## Contract Checklist

- [x] Read source-of-truth files: `AGENTS.md`, `docs/PRD.md`, `docs/GOAL.md`, `build.gradle`, `Dockerfile`.
- [x] Read active stage plans: `docs/Stage3.md` through `docs/Stage9-e.md`.
- [x] Start from `git status --short`: clean at recon start.
- [x] Work on a dedicated branch instead of `master`/`main`.
- [x] Establish baseline non-GUI validation.
- [ ] Close or classify the remaining Stage 3-7 blockers before any Stage 8/9 parity claim.
- [ ] Finish Stage 8 client bootstrap, GUI, render, FX, and keybinding work.
- [ ] Finish Stage 9 recipe/content/research population.
- [ ] Complete Phase 10 polish and final non-GUI validation.
- [ ] Record GUI/manual checks as skipped where interaction or DISPLAY is unavailable.
- [ ] End with clean `git status --short` after the intended checkpoint commits.

## Current Evidence

- `git status --short` was clean at recon start.
- Active branch: `codex/durable-goal-stage8-9`.
- Stage 3/4 server/common baselines are documented as partial; Stage 5-7 remain open in targeted areas.
- `./scripts/dev.sh validate` and `./scripts/dev.sh validate --smoke` pass on current builds.
- GUI/manual graphics checks remain excluded by instruction and headless environment limits.

## Skipped GUI/Manual Graphics Checks

- Interactive GUI, renderer, screenshot, and manual playthrough checks remain skipped until a real client session is available; record future ones as `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.

## Baseline Validation

- `./scripts/dev.sh compileJava` — passed.
- `./scripts/dev.sh check-jar` — passed.
- `./scripts/dev.sh validate` — passed.
- `./scripts/dev.sh validate --smoke` — passed, including server readiness and crash-marker checks.

## Checkpoint Digest

| Area | Condensed status |
| --- | --- |
| Stage 9-e | Research/lang corpus, category/icon baseline, static graph checks, clue unlocks, completion guards, expanded BASICS+THAUMATURGY slices (including infusion-dependent focus, rod/cap, staff, node/vis/focal-manipulation, and wand-pedestal branches), plus ALCHEMY/ARTIFICE/GOLEMANCY/ELDRITCH prerequisites (including `RESEARCH`/`KNOWFRAG`/`THAUMONOMICON`/`ORE`/`PLANTS`, alchemy transport chain `TUBES`/`TUBEFILTER`/`ESSENTIACRYSTAL`/`CENTRIFUGE`/`THAUMATORIUM` with list-backed layout key populated, `BASICARTIFACE` (recipe-backed `PrimalCharm`/`MirrorGlass` pages), `ARCANESTONE`/`TABLE`/`ARCTABLE`/`RESTABLE`/`THAUMOMETER`/`PAVETRAVEL`/`PAVEWARD`/`GOGGLES`/`ARCANEEAR`/`SINSTONE`/`LEVITATOR`/`INFERNALFURNACE` with list-backed layout key populated/`BELLOWS`/`ARCANEBORE`/`ARCANELAMP`/`LAMPGROWTH`/`LAMPFERTILITY`/`FLUXSCRUB` (recipe-backed `FluxScrubber` page)/conditional mirror branch `MIRROR`/`MIRRORHAND`/`MIRRORESSENTIA`/`JARBRAIN`/`INFUSIONENCHANTMENT`/`ARMORFORTRESS`/`HELMGOGGLES`/mask branch `MASKGRINNINGDEVIL`/`MASKANGRYGHOST`/`MASKSIPPINGFIEND`/`BOOTSTRAVELLER`/`HOVERHARNESS`/`HOVERGIRDLE`/`ENCHFABRIC`/`RUNICARMOR`/`RUNICCHARGED`/`RUNICHEALING`/`RUNICKINETIC`/`RUNICEMERGENCY`/`RUNICAUGMENTATION`/`BANNERS`/`ELEMENTAL*`/conditional `WARDEDARCANA`/`BONEBOW`/`PRIMALARROW`/`HUNGRYCHEST`/tiny golem decoration branch `TINYHAT`/`TINYGLASSES`/`TINYBOWTIE`/`TINYFEZ`/`TINYDART`/`TINYVISOR`/`TINYARMOR`/`TINYHAMMER` and golem progression branch with recipe-backed entries `GOLEMSTRAW`/`GOLEMWOOD`/`GOLEMCLAY`/`GOLEMSTONE`/`GOLEMIRON`/`GOLEMTHAUMIUM`/`GOLEMFLESH`/`GOLEMTALLOW`/`GOLEMBELL`/`GOLEMFETTER`/`UPGRADEAIR`/`UPGRADEEARTH`/`UPGRADEFIRE`/`UPGRADEWATER`/`UPGRADEORDER`/`UPGRADEENTROPY`/`TRAVELTRUNK`/`COREGATHER`/`COREFILL`/`COREEMPTY`/`CORESORTING`/`COREUSE`/`COREHARVEST`/`COREFISHING`/`CORELUMBER`/`COREGUARD`/`COREBUTCHER`/`CORELIQUID`/`COREALCHEMY`/`ADVANCEDGOLEM`, alchemy progression branch `TALLOW`/`ALCHEMICALDUPLICATION`/`ALCHEMICALMANUFACTURE`/`ENTROPICPROCESSING`/`LIQUIDDEATH`/`BOTTLETAINT`/`PUREIRON`/`PUREGOLD`/conditional `PURECOPPER`/`PURETIN`/`PURESILVER`/`PURELEAD`/`TRANSIRON`/`TRANSGOLD`/conditional `TRANSCOPPER`/`TRANSTIN`/`TRANSSILVER`/`TRANSLEAD`/`ETHEREALBLOOM`/`BATHSALTS`/`SANESOAP`/`JARLABEL`/`ARCANESPA`/`JARVOID` (recipe-backed pages for `ARCANESPA`/`JARVOID` and `WardedJar` in `JARLABEL`), cluster recipe keys `Clusters0..6` populated for `ORE`, and `THAUMATURGY SCEPTRE` plus `NODEJAR`, `ELDRITCHMINOR`/`OCULUS`/Outer chain/`ADVALCHEMYFURNACE` with list-backed layout key populated/`VOIDMETAL`/`ESSENTIARESERVOIR`/`PRIMALCRUSHER`/`ARMORVOIDFORTRESS`/`FOCUSPRIMAL`/`ROD_primal_staff`) are partially ported. |
| Stage 9-c | Infusion crafting/enchanting, runic augment, recipe-type localization, and inventory-source alignment are partially ported. |
| Stage 9-b | Arcane workbench/container behavior, static/dynamic arcane recipes, and recipe-key audits are partially ported. |
| Stage 8-a/b | Client bootstrap, keybindings, GUI baselines, textures, and rendering scaffolding are partially ported. |
| Stage 7 | Worldgen, Outer Lands, portal, boss-room, and room-template work remains partial and runtime-unverified. |
| Stage 6 | Golem, trunk, pech, loot, projectile, and boss behavior is substantially ported but still has manual runtime gaps. |
| Stage 5 | Hover Harness and utility item behavior are partially ported; client/manual checks remain open. |
| Docs/validation | Smoke wrapper, validation wrapper, and status-refresh notes are done. |

Latest Stage 9-e delta: server-side research-note hex flow baseline is now wired (`HexUtils`, `ResearchNoteData` hex maps, `ResearchManager` note-grid creation + completion checks, `TileResearchTable.placeAspect`, `PacketAspectPlaceToServer` routing, and reference-style `PacketAspectCombinationToServer` aspect-consumption + knowledge-sync behavior), research asset coverage was tightened (`textures/blocks/alchemyblock.png`, `textures/misc/eldritchajor1.png`, `textures/misc/eldritchajor2.png` copied from `thaumcraft_src`), static tests now enforce that `ConfigResearch` `ResourceLocation` paths plus `en_us.lang` `<IMG>` paths exist, packet serialization coverage now includes research-table packets `PacketAspectPlaceToServer` and `PacketAspectCombinationToServer`, hidden-clue entity trigger matching now normalizes legacy vs namespaced IDs (`Thaumcraft.*` ↔ `thaumcraft:*` / `minecraft:*`), recipe-key coverage is now enforced by test for direct `ConfigResearch recipes.get(\"...\")` references against `ConfigRecipes` registrations, and `ConfigAspects` now includes a minimal trigger-critical entity-aspect baseline with static enforcement that every `setEntityTriggers(...)` key resolves to a `registerEntityTag(...)` entry.
Incremental Stage 9-e delta: packet serialization coverage now also includes `PacketPlayerCompleteToServer` round-trip fields (`key`, `dim`, `username`, `type`) to guard primary/secondary research-action payload integrity, and `PacketPlayerCompleteToServerStaticGuardTest` now enforces prerequisite/type-gating + primary-note path presence in the handler source.
Incremental Stage 9-e delta: `ConfigResearchAspectTriggerCoverageTest` now enforces that every `setAspectTriggers(...)` reference in `ConfigResearch` maps to a declared `Aspect` constant, hardening hidden/lost clue trigger integrity against typo regressions.
Incremental Stage 9-d delta: thaumatorium brainbox capacity baseline is now wired (`TileBrainbox` added + registered, `BlockMetalDevice` metadata `12` now instantiates brainbox tiles and aligns facing toward adjacent thaumatorium blocks, `TileThaumatorium#getUpgrades()` restored and invoked on the 40-tick heat/update cadence with capacity shrink-pruning for `recipeHash`/`recipeEssentia`/`recipePlayer`), guarded by static contract tests `ThaumatoriumBrainboxUpgradeContractTest` and `BlockMetalDeviceBrainboxContractTest`, with passing `validate --smoke`.
Incremental Stage 9-d delta: alchemical smelting bonus baseline is now restored in `ConfigRecipes` (`initializeSmeltingBonusBaseline()` wired into init with ore-dictionary mappings for gold/iron/cinnabar/copper/tin/silver/lead, native cluster mappings, and raw meat/fish mappings to `itemNuggetEdible`), guarded by `ConfigRecipesSmeltingBonusCoverageTest`, with passing `validate --smoke`.
Incremental Stage 9-d delta: Thaumcraft alchemy object-tag baseline has been expanded in `ConfigAspects` (`registerThaumcraftAlchemyBaseline()` for shard/nugget/custom-ore/custom-plant/cosmetic/metal-device/essence/resource/zombie-brain tags used by crucible/alchemy flow), guarded by `ConfigAspectsAlchemyTagCoverageTest`, with passing `validate --smoke`.
Incremental Stage 9-d delta: crucible alchemy registration corpus has been expanded in `ConfigRecipes` (restored `BalancedShard_0..5`, `Alt*` duplication/manufacture/entropic recipes, `Pure*`/`Trans*` ore chains, `EtherealBloom`/`LiquidDeath`/`BottleTaint`, `CoreGather`, `BathSalts`, `SaneSoap`, plus taint-essence catalyst payload for `BottleTaint`), guarded by `ConfigRecipesCrucibleAlchemyCoverageTest`, with passing `validate --smoke`.
Incremental Stage 9-d delta: crucible/thaumatorium research-gate contracts are now statically guarded (`CrucibleResearchGateStaticGuardTest` enforces research-completion checks in `findMatchingCrucibleRecipe(...)` and `ContainerThaumatorium` programming plus specificity tie-break presence), with passing `validate`.
Incremental Stage 9-d delta: `ThaumcraftApi.getCraftingRecipeKey(...)` crucible-page lookup contracts are now statically guarded (`ThaumcraftApiCrucibleRecipeKeyStaticGuardTest` enforces the `CrucibleRecipe[]` page branch, key-cache write, and research-gated return path), with passing `validate`.
Incremental Stage 9-d runtime delta: dedicated `smoke-server` baseline rerun after latest alchemy/gating/brainbox/tag updates passes Forge ready-state and crash-marker sweep; manual crucible/thaumatorium progression scenarios remain open.
Incremental Stage 9-d delta: `TileCrucible.attemptSmelt` contracts are now statically guarded (`TileCrucibleSmeltContractStaticGuardTest` enforces thrower-NBT fallback, research matcher call path, fixed 50 mB drain on craft, and no-aspect reject early-return behavior), with passing `validate`.
Incremental Stage 8-e delta: client fallback FX proxy helpers (`blockSparkle`/`beam`/`bolt`/`burst`/`wispFX3`/`sparkle`/`particleCount`) are now implemented, and active FX packets `PacketFXVisDrain` + `PacketFXBlockArc` now schedule client handlers instead of no-op paths, guarded by `ClientProxyFxStaticGuardTest` and `PacketFXSerializationTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXBlockSparkle` now has reference-shaped payload serialization + client-scheduled proxy dispatch, and `ServerTickEventsFML` block-swap flow now sends this packet again (`sendToAllAround`) for replacement sparkle feedback; guarded by expanded `PacketFXSerializationTest` + `ClientProxyFxStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXShield` now has serialization + client-scheduled fallback handler routing, and `EventHandlerRunic` player/champion shield paths now send this packet again; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXSonic` now has serialization + client-scheduled fallback handler routing, and `EntityEldritchGuardian` sonic attack path now broadcasts this packet again; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXWispZap` now has serialization + client-scheduled fallback handler routing, and `EntityWisp` attack path now broadcasts this packet again in the reference attack window; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXZap` now has serialization + client-scheduled fallback handler routing, and `FocusShock.chainLightning` now broadcasts this packet again on each center→closest chain hop with the reference 64-block target-point radius; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXBlockZap` now has serialization + client-scheduled fallback handler routing (including client zap sound), and active tile send-sites are restored in `TileEldritchTrap.update` and `TileInfusionMatrix.inEvZap`; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXEssentiaSource` now has serialization + client-scheduled fallback handler routing (beam-style source stream), and `EssentiaHandler.drainEssentia` now broadcasts this packet again on successful aspect drain with the reference 32-block target-point radius; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXInfusionSource` now has serialization + client-scheduled fallback handler routing for both entity-target and pedestal-offset stream modes, and `TileInfusionMatrix` now broadcasts this packet again in XP-drain and ingredient-pull wind-up paths; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXBlockDig` and `PacketFXBlockBubble` now have serialization + client-scheduled fallback handler routing (block/item dig particle paths plus bubble particle path), guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXBeamPulse` and `PacketFXBeamPulseGolemBoss` now have serialization + client-scheduled fallback handler routing through proxy beam paths (including dual-color golem-boss pulse fallback), guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: missing Stage 8-e misc FX texture atlases/overlays were copied from `thaumcraft_src/assets/thaumcraft/textures/misc/` into `src/main/resources/assets/thaumcraft/textures/misc/`, and `FxTextureAssetCoverageTest` now enforces that baseline asset set in non-GUI validation (`validate --smoke` passing).
Incremental Stage 8-e delta: `PacketBoreDig` now has serialization + client-scheduled handler routing, and `TileArcaneBore` now broadcasts/consumes packed dig replay events (`sendDigEvent`/`getDigEvent`) with fallback client crack-particle dig FX; guarded by new `PacketBoreDigSerializationTest` plus expanded static guard coverage with passing `validate --smoke`.
Incremental Stage 8-e delta: runic shield sound hooks are now restored in `EventHandlerRunic` for player heal/emergency recharge and champion/eldritch shield-hit reactions (`TCSounds.RUNICSHIELDEFFECT` / `TCSounds.RUNICSHIELDCHARGE`), guarded by expanded static coverage with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketMiscEvent` and `PacketWarpMessage` now have serialization + client-scheduled handler baselines, and `WarpEvents` now re-sends warp/mist/sticky-warp client signals (`PacketMiscEvent(0/1)` and `PacketWarpMessage(type=1, data=-1)`), guarded by new `PacketMiscWarpSerializationTest` plus expanded static coverage with passing `validate --smoke`.
Incremental Stage 8-e delta: `EntityEldritchGuardian` now restores its periodic short-mist client signal send (`PacketMiscEvent((short)2)`) for nearby players outside Outer Lands, guarded by expanded static coverage with passing `validate --smoke`.
Incremental Stage 8-e delta: `TileCrucible` client FX hooks are now restored (`drawEffects` + `receiveClientEvent` sparkle/boil routing), `CommonProxy`/`ClientProxy` now include fallback crucible FX helper methods (`crucibleFroth`, `crucibleFrothDown`, `crucibleBubble`, `crucibleBoilSound`, `crucibleBoil`), and static guards now lock both hook presence and placeholder removal (`TileCrucibleSmeltContractStaticGuardTest`, `ClientProxyFxStaticGuardTest`) with passing `validate --smoke`.
Incremental Stage 8-e delta: `EntityEldritchGuardian` now restores its client ambient wisp trail proxy hook (`Thaumcraft.proxy.wispFXEG(...)`) with reference-shaped jittered coordinates, `CommonProxy`/`ClientProxy` now expose fallback `wispFXEG(...)`, and static coverage now guards both client override presence and guardian call-site presence (`ClientProxyFxStaticGuardTest`) with passing `validate --smoke`.
Incremental Stage 8-e delta: `EntityFallingTaint` now restores its client landing FX proxy hook loop (`Thaumcraft.proxy.taintLandFX(this)` on landing/first-fall tick), `CommonProxy`/`ClientProxy` now expose fallback `taintLandFX(Entity)`, and static coverage now guards both client override presence and falling-taint call-site presence (`ClientProxyFxStaticGuardTest`) with passing `validate --smoke`.
Incremental Stage 8-e delta: `ServerTickEventsFML` block-swap paths now restore reference-shaped silk-harvest and fortune-drop handling (`canSilkHarvest` + `getDrops`) and restore the swap-queue wand sound cue (`world.playSound(..., TCSounds.WAND, SoundCategory.PLAYERS, 0.25f, 1.0f)`), with static guard coverage in `ClientProxyFxStaticGuardTest` and passing `validate --smoke`.
Incremental Stage 8-e delta: `TileSensor` note-event buffering hooks are now restored (`noteBlockEvents` weak-map baseline, `EventHandlerWorld` note capture + unload cleanup, `ServerTickEventsFML` per-tick buffer clear), guarded by new `TileSensorNoteEventStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `EventHandlerWorld.onItemCrafted(...)` now restores reference-shaped special crafting return hooks for `itemResource:13` (`ItemEssence` matrix returns) and `blockMetalDevice:3` (center-slot return), guarded by new `EventHandlerWorldCraftingReturnStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `EventHandlerEntity.onItemPickup(...)` now restores the reference fake-player pickup cancel guard for `FakeThaumcraft*` actors, guarded by new `EventHandlerEntityPickupStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `EventHandlerWorld.onHarvestDrops(...)` now restores reference-shaped special-mining replacement flow (tool/focus gate, fortune/treasure scaling, and `Utils.findSpecialMiningResult(...)` drop replacement), while `Utils` now restores `specialMiningResult`/`specialMiningChance` maps plus `addSpecialMiningResult(...)` and `findSpecialMiningResult(...)`; guarded by new `EventHandlerWorldHarvestDropsStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `EventHandlerEntity.onEntityInteract(...)` now restores the reference golem owner-check cancel guard (`EntityGolemBase` + non-owner message/cancel path), guarded by new `EventHandlerEntityInteractStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: champion modifier `showFX(...)` hooks are now restored across the full TODO cluster (`ChampionModArmored/Bold/Fire/Grim/Infested/Mighty/Poison/Sickly/Spined/Undying/Vampire/Warded/Warp`) via proxy fallback routing, and `CommonProxy`/`ClientProxy` now expose `drawGenericParticles(...)` + `slimeJumpFX(...)` helper surface; guarded by new `ChampionModFxStaticGuardTest` plus expanded `ClientProxyFxStaticGuardTest`, with passing `validate --smoke`.
Incremental Stage 8-d delta: `ClientProxy.setupEntityRenderers()` now registers client-side handlers for every `ConfigEntities.ENTITIES` entry via `RenderingRegistry`, using the new `RenderNoop` baseline renderer to eliminate empty-registration risk while full renderer/model parity remains open; guarded by new `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: missing Stage 8-d renderer asset corpus (`textures/models`, `textures/entity`, `textures/misc`, including `.obj`/`.mtl`/`.mcmeta`) is now copied from `thaumcraft_src/assets/thaumcraft/` into `src/main/resources/assets/thaumcraft/`, with new `EntityRendererAssetCoverageTest` guarding critical entity-renderer baseline paths under non-GUI validation (`validate --smoke` passing).
Incremental Stage 8-d delta: item-like entity rendering now uses non-noop `RenderEntityItem` registrations (`EntitySpecialItem`, `EntityPermanentItem`, `EntityFollowingItem`, `EntityItemGrate`) while remaining entities continue through fallback `RenderNoop`; static guard coverage was expanded in `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: projectile entity baseline rendering now uses non-noop `RenderSnowball` registrations (`EntityDart`, `EntityPrimalArrow`, `EntityBottleTaint`, `EntityAlumentum`, `EntityPrimalOrb`, `EntityFrostShard`, `EntityPechBlast`, `EntityEldritchOrb`, `EntityGolemOrb`, `EntityShockOrb`, `EntityExplosiveOrb`, `EntityEmber`, `EntityGolemBobber`) with `ConfigItems`-aware fallback binding for `itemPrimalArrow`/`itemBottleTaint`; guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: compatible zombie/spider groups now use non-noop vanilla fallback renderers (`RenderZombie`/`RenderSpider`) instead of `RenderNoop`, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: taint animal-like groups now use non-noop typed fallback rendering via new `RenderFallbackLiving` (`EntityTaintChicken`/`EntityTaintCow`/`EntityTaintPig`/`EntityTaintSheep`/`EntityTaintVillager`/`EntityTaintCreeper`) with TC model-texture paths, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` and passing `validate --smoke`.
Incremental Stage 8-d delta: cultist entity group now uses non-noop typed biped fallback rendering via new `RenderFallbackBiped` (`EntityCultistKnight`/`EntityCultistCleric`/`EntityCultistLeader`) with TC texture baseline, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` and passing `validate --smoke`.
Incremental Stage 8-d delta: remaining monster baseline now uses non-noop fallback renderer registrations (`EntityFireBat`/`EntityWisp`/`EntityPech`/`EntityEldritchGuardian`/`EntityEldritchWarden`/`EntityEldritchGolem`/`EntityEldritchCrab`/`EntityThaumicSlime`/`EntityTaintSpore`/`EntityTaintSporeSwarmer`/`EntityTaintSwarm`/`EntityTaintacle`/`EntityTaintacleSmall`/`EntityTaintacleGiant`) via `RenderFallbackLiving`/`RenderFallbackBiped` with existing TC texture baselines, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` and passing `validate --smoke`.
Incremental Stage 8-d delta: remaining special entity classes now also have non-noop baseline registrations (`EntityAspectOrb`/`EntityFallingTaint`/`EntityGolemBase`/`EntityTravelingTrunk`/`EntityCultistPortal`) via `RenderSnowball`/`RenderFallbackLiving`/`RenderFallbackBiped`, so the entire current `ConfigEntities.ENTITIES` set has explicit non-noop renderer coverage; guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: coverage hardening test `ClientProxyEntityRendererCoverageByConfigTest` now enforces that every `makeEntry(Entity*.class)` in `ConfigEntities` has explicit `registerEntityRenderer(Entity*.class, ...)` coverage in `ClientProxy`, preventing silent fallback-to-noop registration drift; `validate --smoke` passing.
Incremental Stage 8-c delta: existing magical leaves client model/resource baseline (`BlockMagicalLeaves#getRenderLayer -> CUTOUT_MIPPED`, `BlockMagicalLeavesItem#getMetadata -> damage & 1`, `blockmagicalleaves` blockstate/models, greatwood/silverwood leaf textures) is now protected by new `BlockMagicalLeavesRenderContractTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: `EntityPech` renderer baseline now uses dedicated `RenderPech` registration (`EntityPech -> RenderPech::new`) with explicit `getPechType()` texture routing across `pech_forage`/`pech_thaum`/`pech_stalker`, and static guard coverage was expanded in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityFireBat` renderer baseline now uses dedicated `RenderFireBat` registration (`EntityFireBat -> RenderFireBat::new`) with explicit vampire texture routing plus devil/vampire scale and hanging transform baselines (`getIsVampire`/`getIsDevil`/`getIsBatHanging`), and static guard coverage was expanded in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTravelingTrunk` renderer baseline now uses dedicated `RenderTravelingTrunk` registration (`EntityTravelingTrunk -> RenderTravelingTrunk::new`) with explicit anger texture routing (`trunk.png` vs `trunkangry.png`) and new `EntityTravelingTrunk#getAnger()` renderer contract accessor, with expanded static guard coverage in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityGolemBase` renderer baseline now uses dedicated `RenderGolemBase` registration (`EntityGolemBase -> RenderGolemBase::new`) with explicit golem-type texture routing across `golem_straw`/`golem_wood`/`golem_tallow`/`golem_clay`/`golem_flesh`/`golem_stone`/`golem_iron`/`golem_thaumium`, with expanded static guard coverage in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityWisp` renderer baseline now uses dedicated `RenderWisp` registration (`EntityWisp -> RenderWisp::new`) with explicit aspect-tinted color routing from `Aspect.getAspect(entity.getWispType())` on `wispy.png` plus render-color reset to white, with expanded static guard coverage in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: eldritch guardian/warden baseline now uses dedicated renderer registrations (`EntityEldritchGuardian -> RenderEldritchGuardian::new`, `EntityEldritchWarden -> RenderEldritchWarden::new`) with dedicated texture paths and warden-specific 1.5x render scale baseline, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityEldritchGolem` baseline now uses dedicated `RenderEldritchGolem` registration (`EntityEldritchGolem -> RenderEldritchGolem::new`) with dedicated texture path and reference-shaped 2.15x render scale baseline, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityEldritchCrab` baseline now uses dedicated `RenderEldritchCrab` registration (`EntityEldritchCrab -> RenderEldritchCrab::new`) with dedicated base texture plus dedicated overlay layer wiring (`crab.png` + `craboverlay.png`), guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.

Latest Stage 6 delta: `EntityCultist` no longer overrides ambient/hurt/death sounds with explicit `null` (restoring base hostile sound inheritance consistent with reference class shape), `EntityCultistSoundContractTest` now guards this contract, `TCSoundsStaticCoverageTest` enforces static key/resource consistency across `TCSounds`, `sounds.json`, and bundled `.ogg` assets, and `MonsterSoundNullOverrideAllowlistTest` now prevents new non-allowlisted `null` sound overrides in Stage 6 monster/boss classes (`test` + `validate` passing).
Incremental Stage 6 delta: `EntityCultist` now persists and restores home position with reference-style `HomeD/HomeX/HomeY/HomeZ` semantics and restores cultist faction targeting contracts (`isOnSameTeam` + `canAttackClass`), guarded by `EntityCultistBehaviorContractTest` with passing `test` + `validate --smoke`.
Incremental Stage 6 delta: `EntityCultistPortalLootPlacementContractTest` now guards stage-0 reward placement against regressions to vanilla chest placeholders, and `Stage6 GAP-7` text is aligned with the current `BlockLoot`/`blockLootCrate` implementation baseline.
Incremental Stage 6 delta: `MonsterSoundConstantCoverageTest` now statically scans `thaumcraft.common.entities.monster` for `TCSounds.*` references and enforces that each referenced sound constant is declared in `TCSounds`, hardening monster/boss sound-link integrity (`test` + `validate` passing).
Incremental Stage 5 delta: `ItemResonator.onItemUseFirst(...)` now restores reference-style tube-face diagnostics via `RayTracer.retraceBlock(...)` + `subHit` face override and restores localized untyped suction fallback (`tc.resonator3`) instead of a hardcoded string, guarded by new `ItemResonatorStaticGuardTest` with passing `validate --smoke`.

## Archive

- Full checkpoint index: `docs/GOAL_PROGRESS.archive.md`

## Next Checkpoint Candidate

- Reduce the remaining Stage 8/9 gaps only where they are still server-safe and easy to validate.
- Prefer low-risk Stage 5/6 helper fixes over new GUI or runtime-heavy work.
- Keep Stage 7 runtime evidence and any interactive client checks explicitly marked open.

Do not mark Stage 6 or Stage 7 complete from this checkpoint alone.
