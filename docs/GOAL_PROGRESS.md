# Durable Goal Progress

Last updated: 2026-05-18
Branch: `codex/durable-goal-stage8-9`

> Condensed with user approval. The verbose checkpoint prose was produced by another agent and moved to `docs/GOAL_PROGRESS.archive.md`.

## Contract Checklist

- [x] Read source-of-truth files: `AGENTS.md`, `docs/PRD.md`, `docs/GOAL.md`, `build.gradle`, `Dockerfile`.
- [x] Read active stage plans: `docs/Stage3.md` through `docs/Stage9-e.md`.
- [x] Start from `git status --short`: clean at recon start.
- [x] Establish baseline non-GUI validation.
- [ ] Close or classify the remaining Stage 3-7 blockers before any Stage 8/9 parity claim.
- [ ] Finish Stage 8 client bootstrap, GUI, render, FX, and keybinding work.
- [ ] Finish Stage 9 recipe/content/research population.
- [ ] Complete Phase 10 polish and final non-GUI validation.
- [ ] Record GUI/manual checks as skipped where interaction or DISPLAY is unavailable.
- [ ] End with clean `git status --short` after the intended checkpoint commits.

## Current Evidence

- `git status --short` was clean at recon start.
- Active branch: `codex/durable-goal-stage8-9`.
- Stage 3/4 server/common baselines are documented as partial; Stage 5-7 remain open in targeted areas.
- `./scripts/dev.sh validate` and `./scripts/dev.sh validate --smoke` pass on current builds.
- GUI/manual graphics checks remain excluded by instruction and headless environment limits.
- Incremental Stage 8-e scan-overlay renderer burst delta: restored the missing client-side scan visualization path by replacing inert `RenderEventHandler` scan placeholders with reference-shaped runtime behavior: `startScan(...)` now samples a bounded `17x17x17` block volume around the target (`scanRange` clamped to `8`), classifies ore/water/lava candidates (`OreDictionary` + `ScanManager.getScanAspects(...)` vis-size sampling), and persists sampled values for timed rendering; `renderLast(...)` now actively renders scan feedback until expiry using (a) animated node-strip pulses from `textures/misc/nodes.png` for ore-rated hits and (b) additive colored face overlays for liquid/material markers, with distance/time alpha falloff and player-bound scan ownership guards. `ClientFxEventLayerStaticGuardTest` was expanded to pin the new scan-overlay contracts. Validation passed via targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientFxEventLayerStaticGuardTest --tests thaumcraft.common.items.equipment.ItemElementalPickaxeStaticGuardTest`, plus checkpoint `./scripts/dev.sh validate` and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 9-e research-corpus guard delta: added aggregate fingerprint guard `ConfigResearchReferenceFingerprintStaticGuardTest` that locks the full research corpus against the original 1.7.10 `ConfigResearch.class` using deterministic SHA-256 fingerprints over the sorted `ResearchItem(key|category)` set (`201` entries) and sorted `tc.research_page.*` page-key set (`294` entries). Reference extraction was verified from `Thaumcraft-1.7.10-4.2.3.5.jar` via CFR during checkpoint prep; non-GUI validation passed via targeted `./scripts/dev.sh gradle test --tests thaumcraft.common.config.ConfigResearchReferenceFingerprintStaticGuardTest --tests thaumcraft.common.config.ConfigResearchStaticGraphTest --tests thaumcraft.common.config.ConfigResearchRecipeKeyCoverageTest --tests thaumcraft.common.config.ConfigResearchAspectTriggerCoverageTest` and checkpoint `./scripts/dev.sh validate`.
- Incremental Stage 8-c tube-renderer fidelity burst delta: restored reference-shaped valve/tube directional visual contracts across `TileTubeValve`, `TileTubeValveRenderer`, `TileTubeBufferRenderer`, and `TileTubeOnewayRenderer` by (1) reintroducing valve-side client animation state (`rotation`) and redstone-toggle squeek feedback path in `TileTubeValve.update()`, (2) switching `TileTubeValveRenderer` from synthetic time-based spin to tile-driven rotation/sink transforms, and (3) restoring center-anchored directional transform chains for buffer/oneway choke-valve renders (including opposite-face orientation for buffer sides). Added aggregate guard `TubeRendererFidelityStaticGuardTest`. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.TubeRendererFidelityStaticGuardTest --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest --tests thaumcraft.client.ClientProxyFxStaticGuardTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-e wisp-zap entity-bolt parity delta: restored the reference proxy bolt contract by adding `bolt(World, Entity, Entity)` to `CommonProxy`/`ClientProxy` and routing `PacketFXWispZap` through `Thaumcraft.proxy.bolt(mc.world, sourceEntity, targetEntity)` instead of inline coordinate-only fallback. Static guard `ClientProxyFxStaticGuardTest` now pins the entity-bolt surface and packet route. Runtime-boundary validation passed via `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-e block-zap packet parity delta: aligned `PacketFXBlockZap` client handler with reference behavior by routing zap emission through `Thaumcraft.proxy.nodeBolt(...)` (instead of generic `proxy.bolt(...)`) while preserving scheduled client execution and zap sound playback; static guard `ClientProxyFxStaticGuardTest` now pins the `nodeBolt` route. Runtime-boundary validation passed via `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-e shader-resource parity delta: restored missing post-processing and Thaumcraft shader assets by copying original files from `thaumcraft_src/assets` into runtime resources (`src/main/resources/assets/thaumcraft/shader/{test.frag,test2.frag,test2.vert}` and `src/main/resources/assets/minecraft/shaders/{post/{blurtc.json,desaturatetc.json,hunger.json,sunscorned.json},program/{bloom2.fsh,bloom2.json,bloomColor.fsh,bloomColor.json,bloomVertexBase.vsh,color_convolve2.fsh,color_convolve2.json}}`). Runtime-boundary validation passed via `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-c alchemy-furnace TESR fidelity burst delta: upgraded `TileAlchemyFurnaceAdvancedRenderer` from billboard fallback to model-driven device rendering by adding `ModelAlchemyFurnaceAdvanced` and switching to textured base + four-side tank panel rendering with animated vis-content overlays (`model.renderLavaPanel`) and a heat/burn-driven front glow path (`drawFurnaceGlowQuad`) keyed by furnace burn progress, while preserving burn/vis texture branch selection (`alch_furnace(_on)` and `alch_furnace_tank(_on)`). Added aggregate guard `AlchemyFurnaceAdvancedRendererFidelityStaticGuardTest`; validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.AlchemyFurnaceAdvancedRendererFidelityStaticGuardTest --tests thaumcraft.client.FluxReservoirRendererFidelityStaticGuardTest --tests thaumcraft.client.VisEnergyRendererFidelityStaticGuardTest --tests thaumcraft.client.TileArcaneLampRendererFidelityStaticGuardTest --tests thaumcraft.client.TableRendererFidelityStaticGuardTest --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-c flux/reservoir TESR fidelity burst delta: replaced billboard/flat fallback rendering for two remaining vis-device TESRs by adding dedicated model classes `ModelFluxScrubber` and `ModelEssentiaReservoir`, then rewriting `TileFluxScrubberRenderer` to orientation-aware cap/tip model rendering (`fluxscrubber.png`) with animated tip bob, and rewriting `TileEssentiaReservoirRenderer` to orientation-aware model shell rendering (`reservoir.png`) plus explicit 3D liquid volume rendering from essentia fullness/color (Aspect-aware tint and alpha) instead of 2D liquid quads. Added aggregate guard `FluxReservoirRendererFidelityStaticGuardTest`; validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.FluxReservoirRendererFidelityStaticGuardTest --tests thaumcraft.client.VisEnergyRendererFidelityStaticGuardTest --tests thaumcraft.client.TileArcaneLampRendererFidelityStaticGuardTest --tests thaumcraft.client.TableRendererFidelityStaticGuardTest --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-c vis-energy TESR fidelity burst delta: upgraded energized-node and workbench-charger renderer paths from flat quad fallbacks to reference-shaped dedicated flows by (1) rewriting `TileNodeEnergizedRenderer` to reuse core node visualization (`TileNodeRenderer` aspect/type/modifier pipeline) plus animated lightning-ring overlay frames from `textures/items/lightningringv.png`, and (2) rewriting `TileMagicWorkbenchChargerRenderer` to model-driven ring/support/crystal rendering (`ModelMagicWorkbenchCharger` on `vis_relay.png`) with parent-validity pulse tinting. To support energized-node rendering without strict `INode` coupling, `TileNodeRenderer` now exposes an overload taking explicit node data (`AspectList`, id seed, type, modifier). Added aggregate guard `VisEnergyRendererFidelityStaticGuardTest`; validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.VisEnergyRendererFidelityStaticGuardTest --tests thaumcraft.client.TileArcaneLampRendererFidelityStaticGuardTest --tests thaumcraft.client.TableRendererFidelityStaticGuardTest --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-c arcane-lamp TESR fidelity burst delta: replaced `TileArcaneLampRenderer` textured-quad fallback with model-driven nozzle rendering using `ModelBoreBase` + `textures/models/Bore.png`, restoring orientation-specific nozzle transforms by lamp-facing (`TileArcaneLamp` and `TileArcaneLampGrowth`) and restoring adjacent-`TileArcaneBoreBase` connector-nozzle rendering via opposite-facing transform at the neighboring block position. Added aggregate guard `TileArcaneLampRendererFidelityStaticGuardTest`; validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.TileArcaneLampRendererFidelityStaticGuardTest --tests thaumcraft.client.TableRendererFidelityStaticGuardTest --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-c table-TESR fidelity burst delta: restored model-driven table-family render baselines by adding `ModelTable` and replacing `TileTableRenderer` billboard fallback with reference-shaped model rendering (`table.png`, metadata-gated orientation/skip behavior for non-base table metas), and by upgrading `TileDeconstructionTableRenderer` from plate-quad fallback to model-based `ModelArcaneWorkbench` deconstruction table rendering (`decontable.png`) with explicit thaumometer ground render plus rotated additive input-item overlay and aspect-icon ring overlay. Added aggregate guard `TableRendererFidelityStaticGuardTest`; validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.TableRendererFidelityStaticGuardTest --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-e dedicated-beam-surface burst delta: restored missing proxy/client FX surfaces and dedicated wrappers for reference beam/bolt utility families by extending `CommonProxy` + `ClientProxy` with `beamCont`/`beamBore`/`beamPower`, `nodeBolt` overloads, `sourceStreamFX`, `essentiaTrailFx`, `blockRunes`, `arcLightning`, and `drawInfusionParticles1..4`; added dedicated classes `FXBeamWand`, `FXBeamBore`, `FXBeamPower`, `FXWispArcing`, `FXEssentiaTrail`, and `FXBlockRunes`; expanded aggregate guard coverage in `ClientProxyFxStaticGuardTest`. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-c/9 content-load resource burst delta: filled missing `models/item` coverage for all Thaumcraft block registry paths from `ConfigBlocks.legacyPath(...)` (27 new JSON models for previously missing block item IDs) and added aggregate guard `ConfigBlockItemModelCoverageTest` to enforce future completeness of block item model JSON presence. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.common.config.ConfigBlockItemModelCoverageTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual graphics validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
- Incremental Stage 8-c TESR fidelity burst delta: restored model-based visual baselines for bellows/research table/mana pod/crystal by adding `ModelManaPod` + `ModelCrystal` and porting reference-shaped transforms/animation/color paths in `TileBellowsRenderer`, `TileResearchTableRenderer`, `TileManaPodRenderer`, and `TileCrystalRenderer` (including orientation mapping, research-scroll tinting from `ResearchManager.getData(...)`, and lightmap-driven glow passes), validated with `./scripts/dev.sh compileJava`, `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, `./scripts/dev.sh validate`, and `./scripts/dev.sh validate --smoke` (server smoke pass).
- Stage 5 wand on-use warding baseline now includes reference-shaped owner-gated wand removal flow for warded pressure-plate/cosmetic blocks and arcane doors (`ItemWandCasting.onItemUseFirst`), with `TileOwned.safeToRemove` toggles and arcane-door drop parity guards.
- Stage 5 wand trigger baseline for `ADVALCHEMYFURNACE` now includes non-stub server activation flow in `WandManager` (research-gated vis consume + multiblock validation around `blockStoneDevice` core + activation block-state swap + sparkle/sound FX), replacing the previous dead branch.

Incremental Stage 8-a packet-side-boundary delta: playerdata client packets now route scheduled client work through proxy-side boundaries (`CommonProxy.scheduleClientTask/getClientPlayer/getClientWorld`, `ClientProxy` client implementations) instead of direct `Minecraft.getMinecraft()` calls from `thaumcraft.common.lib.network.playerdata`, covering warp/research/aspect/scan/runic sync handlers; guarded by new `PlayerDataPacketClientBoundaryStaticGuardTest` and updated `ClientProxyFxStaticGuardTest` (`PacketWarpMessage` expectation moved to proxy scheduling), with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `218/218`, `jar`, `check-jar` summary `7079` leaks `1238` unique, `smoke-server` ready-state pass).
Incremental Stage 8-a packet-side-boundary delta: `network/misc` client packet handlers (`PacketMiscEvent`, `PacketBoreDig`, `PacketNote`) now route scheduled client work through proxy-side boundaries (`Thaumcraft.proxy.scheduleClientTask/getClientPlayer/getClientWorld`) instead of direct `Minecraft.getMinecraft()` calls in `thaumcraft.common`, guarded by new `PacketMiscClientBoundaryStaticGuardTest` and updated `ClientProxyFxStaticGuardTest` scheduling assertions (`PacketMiscEvent`/`PacketBoreDig`), with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `219/219`, `jar`, `check-jar` summary `7071` leaks `1238` unique, `smoke-server` ready-state pass).
Incremental Stage 8-c banner subsystem delta: restored `TileBanner` state/NBT baseline (`facing`/`color`/`aspect`/`wall`), restored banner placement/name semantics in `BlockWoodenDeviceItem` (wall-vs-standing facing + color/aspect stack NBT restore + colored translation key suffix), restored banner interaction/drop contracts in `BlockWoodenDevice` (creative colored variants, sneak clear/apply-aspect via `ItemEssence`, and banner drop NBT preservation), and added client TESR burst coverage for a new `TileBannerRenderer` + `ModelBanner` binding in `ClientProxy`; validated by targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest` and full `./scripts/dev.sh validate --smoke` pass.
Incremental Stage 8-c portable-hole/warded renderer burst delta: restored dedicated client TESR wiring for deferred visual wrappers by adding `TileHoleRenderer` (animated tunnel/particlefield directional plane rendering around `blockHole` openings, with distance-sensitive layered/fallback paths) and `TileWardedRenderer` (focus-gated ward overlay rendering while holding a wand with `FocusWarding`, owner-colored additive shell, and ward-cluster face suppression against same-owner adjacent warded blocks), plus new `ClientProxy.setupTileRenderers(...)` bindings for `TileHole` and `TileWarded`. Aggregate Stage 8-c coverage was extended in `ClientProxyTileRendererBurstCoverageTest`; validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and checkpoint `./scripts/dev.sh validate`. GUI/manual render validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c tile-renderer burst delta: expanded `ClientProxy.setupTileRenderers(...)` coverage with dedicated TESR registrations for arcane lamps/workbench tables (`TileArcaneLamp`/`TileArcaneLampGrowth`/`TileArcaneWorkbench`/`TileDeconstructionTable`/`TileResearchTable`), mirror surfaces (`TileMirror`/`TileMirrorEssentia`), tube variants and centrifuge/chest (`TileTubeBuffer`/`TileTubeValve`/`TileTubeOneway`/`TileCentrifuge`/`TileChestHungry`), crystal/reservoir/mana infrastructure (`TileCrystal`/`TileEldritchCrystal`/`TileEssentiaReservoir`/`TileManaPod`/`TileMagicWorkbenchCharger`), and added corresponding renderer classes under `thaumcraft.client.renderers.tile.*`. Static guard coverage in `ClientProxyTileRendererBurstCoverageTest` now asserts the new bindings; validation passed via `./scripts/dev.sh compileJava`, `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, `./scripts/dev.sh validate`, and `./scripts/dev.sh validate --smoke` (server smoke pass). `./scripts/dev.sh smoke-client` remained `SKIPPED: DISPLAY is not set`.
Incremental Stage 8-e dedicated-packet FX burst delta: removed fallback proxy beam/bolt routes for targeted packet handlers by adding dedicated `FXArc` and extending `FXBeam` with packet-driven style controls (`type`, `reverse`, `pulse`), then wiring `PacketFXBlockArc`/`PacketFXBeamPulse`/`PacketFXBeamPulseGolemBoss` to schedule direct `ParticleEngine.addEffect(...)` paths using `FXArc`, `FXBeam`, and `FXBeamGolemBoss`. Static guard coverage was updated in `ClientProxyFxStaticGuardTest`; validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`. Runtime smoke for this sub-checkpoint remained deferred per `docs/GOAL.md` quota-saving burst policy (non-boundary checkpoint).
Incremental Stage 8-e dedicated-packet FX burst delta: upgraded remaining active FX packet handlers from proxy-only fallback routing to dedicated client FX paths by wiring `PacketFXVisDrain`, `PacketFXEssentiaSource`, `PacketFXInfusionSource` to `FXBeam` + `ParticleEngine`, `PacketFXZap`/`PacketFXWispZap` to `FXLightningBolt` + `ParticleEngine`, and `PacketFXShield`/`PacketFXSonic` to direct `FXShieldRunes`/`FXSonic` + `ParticleEngine` emission (with lightweight proxy fallbacks retained where already used by existing non-GUI behavior). `ClientProxyFxStaticGuardTest` expectations were updated for dedicated routing. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, `./scripts/dev.sh validate`, and `./scripts/dev.sh validate --smoke` (server smoke pass).
Incremental Stage 8-c eldritch/infusion TESR burst delta: restored additional missing TESR coverage by adding dedicated renderers `TileInfusionPillarRenderer`, `TileEldritchPortalRenderer`, `TileEldritchCapRenderer` (shared altar/cap path with altar-eye item rendering), `TileEldritchObeliskRenderer`, and `TileEldritchCrabSpawnerRenderer`, plus new `ClientProxy.setupTileRenderers(...)` bindings for `TileInfusionPillar`, `TileEldritchAltar`, `TileEldritchCap`, `TileEldritchObelisk`, `TileEldritchCrabSpawner`, and `TileEldritchPortal`; validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual render validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c remaining-TESR burst delta: restored another clustered set of missing tile renderers by adding dedicated renderers `TileAlchemyFurnaceAdvancedRenderer`, `TileBellowsRenderer`, `TileTableRenderer`, `TileWandPedestalRenderer`, `TileNodeEnergizedRenderer`, `TileFluxScrubberRenderer`, `TileEtherealBloomRenderer`, `TileEldritchLockRenderer`, and `TileEldritchNothingRenderer`, and wiring corresponding `ClientProxy.setupTileRenderers(...)` binds for `TileAlchemyFurnace`, `TileBellows`, `TileTable`, `TileWandPedestal`, `TileNodeEnergized`, `TileFluxScrubber`, `TileEtherealBloom`, `TileEldritchLock`, and `TileEldritchNothing`; aggregate Stage 8-c static coverage in `ClientProxyTileRendererBurstCoverageTest` was updated accordingly. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and runtime-boundary `./scripts/dev.sh validate --smoke` (server smoke pass). GUI/manual render validation remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 9 thaumatorium venting parity delta: restored missing Thaumatorium vent-FX runtime path by adding proxy-side vent particle emission hook (`CommonProxy.drawVentParticles` + `ClientProxy` implementation), reactivating the client-side `venting` tick branch in `TileThaumatorium.update()` with facing-based emission vectors, and restoring output-eject block-event trigger in `completeRecipe()` (`world.addBlockEvent(...)`) so client vent state is actually signaled; static guard coverage in `ThaumatoriumBrainboxUpgradeContractTest` now enforces both event and client vent path contracts. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.common.tiles.ThaumatoriumBrainboxUpgradeContractTest`, `./scripts/dev.sh validate`, and `./scripts/dev.sh validate --smoke` (server smoke pass).
Incremental Stage 8-e ward/portable-hole FX burst delta: added dedicated client particle emitter `FXBlockWard` and routed `ClientProxy.blockSparkle(...)` for warding-sensitive colors (`0xFC9A00` from `FocusWarding` and `0x400040` from `FocusPortableHole`) through `ParticleEngine.addEffect(...)` with block-centered face-biased emission, while preserving existing generic sparkle fallback for other colors. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts both the new routing and `FXBlockWard` baseline. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e crucible-particle FX burst delta: restored dedicated crucible bubble/froth emitter surface by adding client particle classes `FXBubble` and `FXBubbleAlt`, routing `ClientProxy.crucibleFroth(...)`, `crucibleFrothDown(...)`, and `crucibleBubble(...)` through `ParticleEngine.addEffect(...)` (with preserved froth tuning hooks `setFroth`/`setFroth2` and colored bubble tinting), and aligning `PacketFXBlockBubble` with reference-shaped proxy routing by dispatching to `Thaumcraft.proxy.crucibleBubble(...)` around all block edges instead of direct inline world particle calls. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts the dedicated bubble classes and proxy-routed packet path. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e bore-dig particle FX burst delta: restored dedicated bore-dig emission routing by adding client particle class `FXBoreParticles` (target-driven block/item crack stream toward bore nozzle), adding proxy-safe surface `CommonProxy.boreDigFx(...)` with client implementation in `ClientProxy`, and rewiring both `PacketFXBlockDig` and `TileArcaneBore.playClientDigFx()` to emit through proxy-routed dedicated bore particles instead of direct inline particle calls in common paths. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts `boreDigFx` proxy surface, `FXBoreParticles` baseline, and proxy-routed `PacketFXBlockDig` handling. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e wisp/sparkle/burst dedicated-particle burst delta: restored dedicated client particle class paths for common ambient/focus FX by adding `FXBurst`, `FXSparkle`, `FXWisp`, and `FXWispEG`, and rewiring `ClientProxy.burst(...)`, `sparkle(...)`, `wispFX3(...)`, and `wispFXEG(...)` to enqueue those emitters through `ParticleEngine.addEffect(...)` instead of inline direct particle-spawn loops. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts the new dedicated class contracts and updated proxy routing. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e generic/vent dedicated-particle burst delta: restored dedicated client emitter routing for generic champion-style particles and thaumatorium vent trails by adding `FXGeneric` and `FXVent`, and rewiring `ClientProxy.drawGenericParticles(...)` plus `drawVentParticles(...)` to enqueue those emitters through `ParticleEngine.addEffect(...)` instead of direct inline world particle loops. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts both dedicated class baselines and proxy method routing updates. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e vis-sparkle dedicated-particle burst delta: restored dedicated non-ward block sparkle path by adding `FXVisSparkle` and routing the generic branch of `ClientProxy.blockSparkle(...)` through `ParticleEngine.addEffect(...)` (warding-sensitive colors remain on `FXBlockWard`), removing the remaining direct inline `REDSTONE` loop from this proxy surface. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts `FXVisSparkle` baseline and updated blockSparkle routing contract. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e dedicated-beam/bolt cleanup burst delta: aligned proxy beam/bolt emission closer to reference dedicated-FX routing by removing legacy inline fallback `spawnParticle(...)` loops from `ClientProxy.beam(...)` and `ClientProxy.bolt(...)`, leaving these paths to enqueue only `FXBeam`/`FXLightningBolt` through `ParticleEngine.addEffect(...)`. Aggregate FX guard coverage in `ClientProxyDedicatedBeamBoltStaticGuardTest` now asserts absence of the old inline loop markers while preserving dedicated-class baseline checks. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e block-sparkle sentinel-color parity burst delta: restored reference random-color sentinel handling for `ClientProxy.blockSparkle(...)` by routing color `-9999` through `FXVisSparkle` random-tint mode (instead of fixed fallback color decode), preserving ward-specific `FXBlockWard` branch and generic color routing. `FXVisSparkle` now carries explicit random-color emission branch (`0.33..1.0` base with variance) for this sentinel path. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts both `-9999` routing and random-color dedicated baseline markers. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e crucible-boil bubble parity burst delta: restored reference-shaped dedicated crucible boil emission by replacing inline vanilla `WATER_BUBBLE`/`CRIT_MAGIC` spawns in `ClientProxy.crucibleBoil(...)` with `FXBubble` routing through `ParticleEngine.addEffect(...)`, including aspect-aware tint selection (or white fallback when empty) and explicit boil intensity scaling via `bubble.setBubbleSpeed(0.003D * type)`. `FXBubble` now exposes `setBubbleSpeed(...)` to preserve this contract. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts the dedicated crucible-boil path and new `FXBubble` speed hook marker. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-c infusion-renderer fidelity burst delta: upgraded the core infusion altar TESR set toward reference behavior by adding `ModelCube`, rewriting `TileRunicMatrixRenderer` to render the rotating 8-cube cluster with instability/startup motion, additive overlay cubes, and deterministic crafting halo rays, and tightening pedestal-family item rendering scale contracts in `TilePedestalRenderer`/`TileWandPedestalRenderer` (`ItemBlock` scale `2.0`, non-block `1.0`). `TileInfusionPillarRenderer` was also upgraded from crossed-quad billboard fallback to a solid textured prism with reference-shaped orientation transforms (x-axis tilt + orientation-driven z-rotation). Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-c alchemy-renderer fidelity burst delta: upgraded core alchemy TESR visuals by adding lightweight model classes `ModelAlembic` and `ModelThaumatorium`, rewriting `TileAlembicRenderer` to render oriented alembic body parts with reference-shaped leg/tube state selection (`aboveFurnace`/`aboveAlembic`), retained aspect-filter labels, and restored nozzle visuals toward non-tube essentia connectable neighbors; rewrote `TileThaumatoriumRenderer` to render oriented thaumatorium model geometry and preserved rotating output-item display path from crucible recipe hashes; and updated `TileCrucibleRenderer` fluid-surface tinting/alpha logic to follow reference recolor behavior while keeping 1.12-safe textured quad rendering. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyTileRendererBurstCoverageTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.
Incremental Stage 8-e residual-smoke dedicated-particle burst delta: removed the last direct inline `SMOKE_NORMAL` world-particle spawns from `ClientProxy` by adding dedicated emitter class `FXSmokeDrift` and routing taint-land smoke puffs (`taintLandFX`) plus crucible froth smoke puffs (`crucibleFroth`) through `ParticleEngine.addEffect(...)`. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts `FXSmokeDrift` baseline and both updated proxy call-sites. Validation passed via `./scripts/dev.sh compileJava`, targeted `./scripts/dev.sh gradle test --tests thaumcraft.client.ClientProxyFxStaticGuardTest --tests thaumcraft.client.ClientProxyDedicatedBeamBoltStaticGuardTest --tests thaumcraft.common.lib.network.fx.PacketFXSerializationTest`, and checkpoint `./scripts/dev.sh validate`; runtime smoke was deferred for this non-boundary burst per `docs/GOAL.md` quota-saving policy.

Incremental Stage 4 node-conversion delta: restored reference-shaped node conversion server/common baseline by porting `TileNodeConverter` state machine (`status`/`count`, powered progression, node-drain countdown, Node↔Energized conversion payload transfer, `checkStatus` failure path, client event burst/sound hook), restored `TileNodeStabilizer` client tick/count/lock/render-bounds baseline, added `BlockAiry.explodify(...)` flux-scatter failure helper, and wired `BlockStoneDevice.neighborChanged(...)` converter status refresh hook; guarded by new `TileNodeConversionStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `177/177`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 7 obelisk/cap tile delta: restored non-stub Eldritch tile baselines by porting `TileEldritchObelisk` server buff loop and client FX hook (`EntityUtils` radius scan for `IEldritchMob`, regeneration+resistance pulse, and proxy wisp trail), plus render-distance/bounds contracts for `TileEldritchObelisk`, `TileEldritchCap`, and `TileEldritchNothing`; guarded by new `TileEldritchTilesStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `179/179`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 4 infusion pillar delta: restored non-stub `TileInfusionPillar` baseline with reference-shaped `orientation` field persistence (`orientation` NBT read/write) and expanded client render bounds (`x/z -1..+1`, `y -1..+2`), guarded by new `TileInfusionPillarStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `180/180`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 4 arcane-furnace-nozzle delta: restored non-stub `TileArcaneFurnaceNozzle` essentia transport baseline (`IEssentiaTransport` surface, adjacent-furnace discovery, 5-tick FIRE draw cadence via `ThaumcraftApiHelper.getConnectableTile(...)`, and `speedyTime` top-up flow), plus minimal `TileArcaneFurnace.speedyTime` backing field required by the nozzle contract; guarded by new `TileArcaneFurnaceNozzleStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `181/181`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 4 arcane-furnace backend delta: restored non-stub `TileArcaneFurnace` server/common smelting baseline (32-slot internal inventory payload, `CookTime`/`SpeedyTime` NBT persistence, FIRE vis speed boost via `VisNetHandler.drainVis(...)`, bellows-influenced cook-time scaling, smelt-result ejection with bonus-output/XP handling, and nozzle-based facing/eject vector contracts), guarded by new `TileArcaneFurnaceStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `183/183`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 4 arcane-furnace block delta: restored missing `BlockArcaneFurnace` registration/placement baseline (`ConfigBlocks.blockArcaneFurnace` init + `getAllBlocks` + metadata ItemBlock registration) and non-stub block behavior surface (`TYPE` metadata `0..10`, `TileArcaneFurnace`/`TileArcaneFurnaceNozzle` tile creation map, low-profile collision for core meta `0`, item-ingest forwarding to `TileArcaneFurnace.addItemsToInventory(...)`, and fire damage contract for living entities colliding with the core), guarded by new `BlockArcaneFurnaceStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `185/185`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 7 greatwood delta: restored reference-shaped natural spider-variant hook in `ThaumcraftWorldGenerator.generateGreatwood(...)` by switching to the explicit overloaded call `WorldGenGreatwoodTrees.generate(..., rand.nextInt(8) == 0)` instead of the no-arg helper path, guarded by new `ThaumcraftWorldGeneratorGreatwoodStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `186/186`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 4 alembic tile delta: restored reference-shaped non-ticking alembic client/update contracts by removing the stub tick surface, restoring expanded render bounds (`x/z -1..+2`, `y +0..+1`), and re-running appearance refresh on tile sync packet receipt (`onDataPacket -> getAppearance()`), guarded by new `TileAlembicStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `188/188`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 4 alembic acceptance delta: restored reference-shaped `TileAlembic.doesContainerAccept(...)` contract to unconditional acceptance (`return true`) while keeping insertion constraints inside `addToContainer(...)`, preventing transport-side routing regressions tied to pre-filtered accept checks; guarded by expanded `TileAlembicStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `188/188`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 5 research-notes UX delta: restored reference-shaped `ItemResearchNotes` display/tooltip contracts by adding guaranteed glint (`hasEffect`), discovery-vs-note display name mapping (`item.discovery.name` / `item.researchnotes.name`), unknown-discovery hint lines, completed-note research name/text tooltip payload, and forbidden-warp tooltip severity text from `ThaumcraftApi.getWarp(...)`; also restored missing `item.researchnotes.name` localization key in `en_us.lang`; guarded by expanded `ItemResearchNotesCoreContractsStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `188/188`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 4 mirror-essentia link delta: restored reference-shaped `TileMirrorEssentia` link invalidation and container semantics by syncing the local tile during remote-link invalidation (`invalidateLink -> this.markDirtyAndSync()`) and restoring the no-op `addToContainer(...)` return contract (`0`), guarded by new `TileMirrorEssentiaStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `190/190`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 5 compass-stone parity delta: restored explicit reference-shaped creative exposure for `ItemCompassStone` (single subtype entry meta `0`) and added guard coverage for sinister-node active visibility/expiry contracts (10-second stale-node pruning, dimension+FOV/range checks, passive right-click behavior), via new `ItemCompassStoneStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `192/192`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 5 item-resource alumentum delta: restored reference-shaped alumentum and knowledge-fragment right-click behavior in `ItemResource` (meta `0` bow-sound throw path with `EntityAlumentum` launch vector `shoot(..., 0.75F, 1.0F)` and meta `9` primal-aspect pool grant/sync loop), restored reference dispenser behavior for alumentum via new `BehaviorDispenseAlumetum` + `Thaumcraft.init` registration (`BlockDispenser.DISPENSE_BEHAVIOR_REGISTRY.putObject(...)`), and corrected `EntityAlumentum` physics split for 1.12 parity (`getGravityVelocity=0.03F`, launch speed now item-driven); guarded by new `ItemResourceAlumentumKnowledgeStaticGuardTest` and updated `EntityAlumentumCoreContractsStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `194/194`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 5 item-resource utility delta: restored reference-shaped nitor/charm behavior in `ItemResource` by porting nitor placement flow to `onItemUse` (`ConfigBlocks.blockAiry` meta `1`, replaceable/offset placement rules, `mayPlace` + `canPlayerEdit` checks, place-sound contract, stack shrink on success) and charm stack-size override (`meta 15` max stack `1`), guarded by expanded `ItemResourceAlumentumKnowledgeStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `194/194`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 5 item-resource utility delta: `ItemResource` nitor placement now aligns no-op use-result semantics with reference boolean-false behavior by returning `PASS` (instead of `FAIL`) on blocked edit/place/set branches in `onItemUse(...)`, guarded by expanded `ItemResourceAlumentumKnowledgeStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 item-resource taint/charm delta: restored reference-shaped taint/charm tick and tooltip contracts in `ItemResource` by porting taint-resource infection handling (`meta 11/12` flux-taint potion apply + poison chat + inventory consume), primal-charm random behavior (`blurb` tag cleanup, primal aspect orb spawn, hidden `@FOCUSPRIMAL` trigger unlock/message path), aspect-tooltip unknown fallback + random charm blurbs, and `getAspects(...)` empty-list null contract; also restored missing `en_us.lang` keys (`tc.taint_item_poison`, `tc.primalcharm.0..4`, `tc.primalcharm.trigger`), guarded by expanded `ItemResourceAlumentumKnowledgeStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `194/194`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 5 item-resource creative-tab delta: restored reference-shaped creative listing contract by excluding `META_BRAIN` from `ItemResource.getSubItems(...)` while keeping all other metas exposed, guarded by expanded `ItemResourceAlumentumKnowledgeStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `194/194`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 4/5 vis-relay wand-contract delta: tightened `TileVisRelay` to match reference wand interaction semantics by restoring default relay orientation baseline (`orientation = 1`), making the `IWandable` right-click overload return `null` (preventing extra generic right-click handling), and restoring the crystal attunement-cycle sound on server color toggles (`TCSounds.CRYSTAL`); guarded by new `TileVisRelayStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `195/195`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).
Incremental Stage 5 wand/utility hardening delta: locked remaining reference baselines for `WandRodPrimalOnUpdate` (single-primal 200-tick recharge and random-primal 50-tick recharge candidate selection under `< maxVis/10` gate) and `ItemBathSalts` (short despawn lifespan `200` ticks) via new `WandRodPrimalOnUpdateStaticGuardTest` and `ItemBathSaltsStaticGuardTest`, with passing `./scripts/dev.sh validate` (`compileJava`, `test` `197/197`, `jar`, `check-jar` summary).
Incremental Stage 6 eldritch-guardian combat/fog delta: restored two reference-shaped server contracts in `EntityEldritchGuardian` by gating periodic fog-pulse packets to non-EASY difficulty (`difficulty != EASY`) and restoring difficulty-scaled burning-melee ignition (`empty main hand`, `isBurning`, chance `difficulty*0.3`, fire duration `2*difficulty`), guarded by new `EntityEldritchGuardianStaticGuardTest`, with passing `./scripts/dev.sh validate --smoke` (`compileJava`, `test` `198/198`, `jar`, `check-jar` summary, `smoke-server` ready-state pass).

## Skipped GUI/Manual Graphics Checks

- Interactive GUI, renderer, screenshot, and manual playthrough checks remain skipped until a real client session is available; record future ones as `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.

## Baseline Validation

- `./scripts/dev.sh compileJava` — passed.
- `./scripts/dev.sh check-jar` — passed.
- `./scripts/dev.sh validate` — passed.
- `./scripts/dev.sh validate --smoke` — passed, including server readiness and crash-marker checks.

## Checkpoint Digest

| Area | Condensed status |
| --- | --- |
| Stage 9-e | Research/lang corpus, category/icon baseline, static graph checks, clue unlocks, completion guards, expanded BASICS+THAUMATURGY slices (including infusion-dependent focus, rod/cap, staff, node/vis/focal-manipulation, and wand-pedestal branches), plus ALCHEMY/ARTIFICE/GOLEMANCY/ELDRITCH prerequisites (including `RESEARCH`/`KNOWFRAG`/`THAUMONOMICON`/`ORE`/`PLANTS`, alchemy transport chain `TUBES`/`TUBEFILTER`/`ESSENTIACRYSTAL`/`CENTRIFUGE`/`THAUMATORIUM` with list-backed layout key populated, `BASICARTIFACE` (recipe-backed `PrimalCharm`/`MirrorGlass` pages), `ARCANESTONE`/`TABLE`/`ARCTABLE`/`RESTABLE`/`THAUMOMETER`/`PAVETRAVEL`/`PAVEWARD`/`GOGGLES`/`ARCANEEAR`/`SINSTONE`/`LEVITATOR`/`INFERNALFURNACE` with list-backed layout key populated/`BELLOWS`/`ARCANEBORE`/`ARCANELAMP`/`LAMPGROWTH`/`LAMPFERTILITY`/`FLUXSCRUB` (recipe-backed `FluxScrubber` page)/conditional mirror branch `MIRROR`/`MIRRORHAND`/`MIRRORESSENTIA`/`JARBRAIN`/`INFUSIONENCHANTMENT`/`ARMORFORTRESS`/`HELMGOGGLES`/mask branch `MASKGRINNINGDEVIL`/`MASKANGRYGHOST`/`MASKSIPPINGFIEND`/`BOOTSTRAVELLER`/`HOVERHARNESS`/`HOVERGIRDLE`/`ENCHFABRIC`/`RUNICARMOR`/`RUNICCHARGED`/`RUNICHEALING`/`RUNICKINETIC`/`RUNICEMERGENCY`/`RUNICAUGMENTATION`/`BANNERS`/`ELEMENTAL*`/conditional `WARDEDARCANA`/`BONEBOW`/`PRIMALARROW`/`HUNGRYCHEST`/tiny golem decoration branch `TINYHAT`/`TINYGLASSES`/`TINYBOWTIE`/`TINYFEZ`/`TINYDART`/`TINYVISOR`/`TINYARMOR`/`TINYHAMMER` and golem progression branch with recipe-backed entries `GOLEMSTRAW`/`GOLEMWOOD`/`GOLEMCLAY`/`GOLEMSTONE`/`GOLEMIRON`/`GOLEMTHAUMIUM`/`GOLEMFLESH`/`GOLEMTALLOW`/`GOLEMBELL`/`GOLEMFETTER`/`UPGRADEAIR`/`UPGRADEEARTH`/`UPGRADEFIRE`/`UPGRADEWATER`/`UPGRADEORDER`/`UPGRADEENTROPY`/`TRAVELTRUNK`/`COREGATHER`/`COREFILL`/`COREEMPTY`/`CORESORTING`/`COREUSE`/`COREHARVEST`/`COREFISHING`/`CORELUMBER`/`COREGUARD`/`COREBUTCHER`/`CORELIQUID`/`COREALCHEMY`/`ADVANCEDGOLEM`, alchemy progression branch `TALLOW`/`ALCHEMICALDUPLICATION`/`ALCHEMICALMANUFACTURE`/`ENTROPICPROCESSING`/`LIQUIDDEATH`/`BOTTLETAINT`/`PUREIRON`/`PUREGOLD`/conditional `PURECOPPER`/`PURETIN`/`PURESILVER`/`PURELEAD`/`TRANSIRON`/`TRANSGOLD`/conditional `TRANSCOPPER`/`TRANSTIN`/`TRANSSILVER`/`TRANSLEAD`/`ETHEREALBLOOM`/`BATHSALTS`/`SANESOAP`/`JARLABEL`/`ARCANESPA`/`JARVOID` (recipe-backed pages for `ARCANESPA`/`JARVOID` and `WardedJar` in `JARLABEL`), cluster recipe keys `Clusters0..6` populated for `ORE`, and `THAUMATURGY SCEPTRE` plus `NODEJAR`, `ELDRITCHMINOR`/`OCULUS`/Outer chain/`ADVALCHEMYFURNACE` with list-backed layout key populated/`VOIDMETAL`/`ESSENTIARESERVOIR`/`PRIMALCRUSHER`/`ARMORVOIDFORTRESS`/`FOCUSPRIMAL`/`ROD_primal_staff`) are partially ported. |
| Stage 9-c | Infusion crafting/enchanting, runic augment, recipe-type localization, and inventory-source alignment are partially ported. |
| Stage 9-b | Arcane workbench/container behavior, static/dynamic arcane recipes, and recipe-key audits are partially ported. |
| Stage 8-a/b | Client bootstrap, keybindings, GUI baselines, textures, and rendering scaffolding are partially ported. |
| Stage 7 | Worldgen, Outer Lands, portal, boss-room, and room-template work remains partial and runtime-unverified. |
| Stage 6 | Golem, trunk, pech, loot, projectile, and boss behavior is substantially ported but still has manual runtime gaps. |
| Stage 5 | Hover Harness and utility item behavior are partially ported; client/manual checks remain open. |
| ConfigRecipes | Refactored from monolithic file into hub + 7 recipe-family slices under `thaumcraft.common.config.recipes/`. Static-guard tests adapted via `ConfigRecipesSourceReader.readMergedSource()`. Init order, recipe keys, research-handle semantics unchanged. |
| Docs/validation | Smoke wrapper, validation wrapper, and status-refresh notes are done. |

Latest Stage 9-e delta: server-side research-note hex flow baseline is now wired (`HexUtils`, `ResearchNoteData` hex maps, `ResearchManager` note-grid creation + completion checks, `TileResearchTable.placeAspect`, `PacketAspectPlaceToServer` routing, and reference-style `PacketAspectCombinationToServer` aspect-consumption + knowledge-sync behavior), research asset coverage was tightened (`textures/blocks/alchemyblock.png`, `textures/misc/eldritchajor1.png`, `textures/misc/eldritchajor2.png` copied from `thaumcraft_src`), static tests now enforce that `ConfigResearch` `ResourceLocation` paths plus `en_us.lang` `<IMG>` paths exist, packet serialization coverage now includes research-table packets `PacketAspectPlaceToServer` and `PacketAspectCombinationToServer`, hidden-clue entity trigger matching now normalizes legacy vs namespaced IDs (`Thaumcraft.*` ↔ `thaumcraft:*` / `minecraft:*`), recipe-key coverage is now enforced by test for direct `ConfigResearch recipes.get(\"...\")` references against `ConfigRecipes` registrations, and `ConfigAspects` now includes a minimal trigger-critical entity-aspect baseline with static enforcement that every `setEntityTriggers(...)` key resolves to a `registerEntityTag(...)` entry.
Incremental Stage 3 sync-authority delta: `PacketRunicCharge` payload contract is now aligned with the 1.7.10 wire shape (`entityId:int`, `charge:short`, `max:short`) and the client handler now mirrors reference direct assignment semantics for charge/max state, guarded by expanded `PlayerDataPacketSerializationTest` (explicit 8-byte wire-size assertion) with passing `validate --smoke`.
Incremental Stage 3 capability-lifecycle delta: `EventHandlerEntity.onPlayerClone(...)` now uses a capability-lifecycle fallback path (`original.getCapability(...)` first, then `ResearchManager.getResearchData(original.getName())` when the original capability is unavailable) before cloning NBT into the new player capability, preserving the clone copy/cache path while hardening death/End clone edge cases; guarded by new `EventHandlerEntityCloneStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 7 persistence-hardening delta: `MazeHandler.loadMaze/saveMaze` now uses scoped `try-with-resources` IO for `labyrinth.dat`/`labyrinth.dat_old` reads and temp writes, and keeps a direct-write fallback path when `labyrinth.dat_new -> labyrinth.dat` rename fails; guarded by new `MazeHandlerPersistenceStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 7 outer-provider parity delta: `WorldProviderOuter` now restores reference-shaped non-hell environment contracts by disabling water vaporization (`doesWaterVaporize() -> false`) and restoring the classic Outer Lands fog baseline (`0xA080A0` tinted to `15%` intensity), guarded by new `WorldProviderOuterStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 7 outer-provider parity delta: `WorldProviderOuter` now restores the reference cloud-height baseline (`getCloudHeight() -> 1.0f` instead of hidden-cloud sentinel), guarded by expanded `WorldProviderOuterStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 7 outer-provider parity delta: `WorldProviderOuter.createChunkGenerator()` now restores the reference constructor-flag contract by passing `mapFeaturesEnabled=true` into `ChunkProviderOuter`, guarded by expanded `WorldProviderOuterStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 relic delta: `ItemThaumonomicon` now restores reference-shaped cheat-sheet and sibling-unlock behavior (`meta 42` creative entry gated by `Config.allowCheatSheet`, EPIC rarity + `Cheat Sheet` tooltip, server-side all-research/all-aspect unlock path for cheat-sheet, sibling completion pass for regular book use, and post-mutation targeted sync via `PacketSyncResearch` + `PacketSyncAspects` with cache refresh) plus client page sound hook, guarded by `ItemThaumonomiconStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 5 relic delta: `ItemHandMirror` now restores reference-shaped link UX contracts by switching GUI open coordinates to `MathHelper.floor(...)`, restoring NBT-driven item glint (`hasEffect`), and restoring linked-destination tooltip output (`tc.handmirrorlinkedto` with `linkX/linkY/linkZ/dimname` payload), guarded by new `ItemHandMirrorStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 5 relic localization delta: `en_us.lang` now includes explicit relic chat/tooltip keys used by current server/common relic paths (`tc.handmirrorlinkedto`, `tc.sanity`), guarded by new `RelicLocalizationStaticGuardTest` asserting both source usage and key presence.
Incremental Stage 5 relic delta: `ItemThaumometer` now restores a reference-shaped timed scan flow on the item side (`25`-tick use duration + `EnumAction.NONE`, `startScan` capture on right-click, client-side stability/completion gate near end-of-use, `ScanManager.completeScan(..., \"@\")` completion check, server-bound `PacketScannedToServer` emit on successful completion, and reset on stop), with scan-target resolution supporting entities, node phenomena, block/item targets, and scan event handlers; guarded by new `ItemThaumometerStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 5 relic delta: `ItemThaumometer` client stability gate now explicitly requires non-null `startScan` before equality comparison, preventing false-positive completions caused by legacy `ScanResult.equals(...)` null semantics; guard coverage was expanded in `ItemThaumometerStaticGuardTest`.
Incremental Stage 5 relic delta: `ItemThaumometer` block-scan path now mirrors reference defensive behavior by wrapping `getPickBlock(...)` in a guarded `try/catch` and falling back to `BlockUtils.createStackedBlock(...)`, reducing scan-time crash risk on problematic block implementations; guarded by expanded `ItemThaumometerStaticGuardTest`.
Incremental Stage 5 relic delta: `ItemThaumometer` completion branch now restores the reference active-use lifecycle contract by calling `player.stopActiveHand()` when the timed scan completes (before completion packet handling), preventing post-completion overuse drift; guarded by expanded `ItemThaumometerStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 5 relic delta: `ItemSanityChecker` now reports capability-backed warp state as both total and explicit breakdown (`perm/sticky/temp`) instead of a single opaque value, with corresponding `en_us.lang` detail localization key (`tc.sanity.detail`); guarded by new `ItemSanityCheckerStaticGuardTest` plus updated `RelicLocalizationStaticGuardTest` coverage.
Incremental Stage 5 utility delta: `ItemBucketDeath` now keeps reference-shaped no-op result semantics (`PASS` for blocked/non-place branches) while preserving full-level `ConfigBlocks.blockFluidDeath` placement and survival empty-bucket return with creative bypass; all three contracts are locked by `ItemBucketDeathStaticGuardTest`.
Incremental Stage 5 relic delta: `ItemHandMirror.transport(...)` spawn-vector and motion-direction contracts are now aligned with the reference orientation path (no `getOpposite()`, spawn offset `- facing*0.3`, motion `+ facing*0.15`), guarded by expanded `ItemHandMirrorStaticGuardTest`.
Incremental Stage 5 relic delta: `ItemHandMirror` action-side contracts are now aligned with the reference flow by restoring `PASS` semantics for no-link/invalid-link right-click branches, restoring the remote `onItemUseFirst` swing+`PASS` mirror-link branch, and restoring link/error feedback tuning (`JAR` `1.0/2.0`, `ZAP` `1.0/0.8`, non-actionbar status chat), guarded by expanded `ItemHandMirrorStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 relic delta: `ItemResonator` client `onItemUseFirst` now mirrors the reference super-fallback semantics (`swingArm` + `PASS` on remote side) instead of returning `SUCCESS` client-side, while preserving server diagnostics/suction chat and knock sound behavior; guarded by expanded `ItemResonatorStaticGuardTest`.
Incremental Stage 5 utility delta: `ItemKey.onItemUseFirst(...)` now restores missing arcane-door key flow alongside wooden-device handling (upper/lower-half base resolution, mirrored access grant propagation to linked door tile, and type-specific feedback keys `tc.key1..tc.key6`) while keeping reference side-result semantics (`PASS` remote handled branches, `SUCCESS` server); guarded by expanded `ItemKeyCoreContractsStaticGuardTest`.
Incremental Stage 5 utility delta: `ItemInkwell.onItemUseFirst(...)` now mirrors reference side-result semantics (`PASS` on remote paired-table branch, `SUCCESS` on server conversion) while preserving paired table mutation (`TYPE 2/3`), scribing-tool insertion into `TileResearchTable`, and survival-stack consumption behavior; guarded by new `ItemInkwellCoreContractsStaticGuardTest`.
Incremental Stage 5 utility delta: `ItemAmuletVis` now restores reference tooltip/localization visibility contracts (`item.ItemAmuletVis.text` hint on lesser variant, localized `item.capacity.text` capacity line, and primal vis amount listing formatted per-aspect) while preserving existing wand/relay vis transfer behavior; guarded by new `ItemAmuletVisCoreContractsStaticGuardTest`.
Incremental Stage 5 baubles delta: runic bauble rarity contracts are now aligned with reference (`ItemAmuletRunic` and `ItemGirdleRunic` always `RARE`, `ItemRingRunic` `UNCOMMON` for lesser meta and `RARE` otherwise), guarded by new `ItemRunicBaublesRarityStaticGuardTest`.
Incremental Stage 5 baubles delta: `ItemBaubleBlanks` now restores aspect-ring UI contracts (`%TYPE` display-name localization via `item.ItemBaubleBlanks.3.name` plus `tc.discount` tooltip line), and `ItemGirdleHover` now mirrors reference subtype/fall-distance behavior (`setHasSubtypes(true)` plus unclamped `fallDistance -= 0.33F`); guarded by new `ItemBaubleBlanksHoverContractsStaticGuardTest`.
Incremental Stage 6 golem-item delta: `ItemGolemPlacer` now restores missing reference UX contracts by re-enabling always-glint + sneak-bypass behavior, restoring NBT-driven tooltip coverage for core/advanced/upgrades/markers/decorations, and aligning server use-first result semantics to consume-success (`SUCCESS` server path, `PASS` remote/sneak) while preserving existing spawn/consume flow; guarded by new `ItemGolemPlacerCoreContractsStaticGuardTest`.
Incremental Stage 6 golem-item delta: `ItemTrunkSpawner` now restores reference tooltip/output UX (`upgrade` name line and `item.TrunkSpawner.text.1` inventory hint), restores spawn-time living sound hook, and aligns use-result semantics to consume-success (`SUCCESS` return on server path while preserving existing spawn/consume gating); guarded by new `ItemTrunkSpawnerCoreContractsStaticGuardTest`.
Incremental Stage 6 golem-item delta: `ItemGolemDecoration` now restores the reference display-name contract by prefixing subtype names with localized accessory label (`item.ItemGolemDecoration.name: ...`) while preserving existing subtype/deco-char mappings (`H/G/B/F/R/V/P/M`); guarded by new `ItemGolemDecorationCoreContractsStaticGuardTest`.
Incremental Stage 6 golem-item delta: `ItemGolemCore` now restores the reference localized tooltip contract (`item.ItemGolemCore.<meta>.name`) while preserving blank-core rarity split (`meta 100` common) and core GUI/sort/inventory capability maps; guarded by new `ItemGolemCoreContractsStaticGuardTest`.
Incremental Stage 6 golem-item delta: `ItemGolemBell` now restores reference helper/UX contracts (home-face/home-pos NBT accessors `golemhomeface` + `golemhomex/y/z`, always-glint hook, side-aware `onItemUseFirst` result `PASS` client / `SUCCESS` server, and creative link-copy behavior), while preserving existing marker/link/pickup logic; guarded by new `ItemGolemBellContractsStaticGuardTest`.
Incremental Stage 6 projectile delta: `EntityDart` now restores reference spawn/trajectory baseline by implementing motion+rotation spawn-data payload (`IEntityAdditionalSpawnData` read/write), adding target-aware constructor ballistics (`lead = horizontal * 0.2F`, `shoot(...)`), and restoring first-tick client smoke burst behavior; guarded by new `EntityDartCoreContractsStaticGuardTest`.
Incremental Stage 6 projectile delta: `EntityAlumentum` now restores the reference low eye-height hook (`getEyeHeight() = 0.1F`) while preserving existing mob-griefing-gated impact explosion (`1.66F`) and launch velocity baseline; guarded by new `EntityAlumentumCoreContractsStaticGuardTest`.
Incremental Stage 9-e delta: packet serialization coverage now also includes `PacketPlayerCompleteToServer` round-trip fields (`key`, `dim`, `username`, `type`) to guard primary/secondary research-action payload integrity, and `PacketPlayerCompleteToServerStaticGuardTest` now enforces prerequisite/type-gating + primary-note path presence in the handler source.
Incremental Stage 9-e delta: `ConfigResearchAspectTriggerCoverageTest` now enforces that every `setAspectTriggers(...)` reference in `ConfigResearch` maps to a declared `Aspect` constant, hardening hidden/lost clue trigger integrity against typo regressions.
Incremental Stage 9-d delta: thaumatorium brainbox capacity baseline is now wired (`TileBrainbox` added + registered, `BlockMetalDevice` metadata `12` now instantiates brainbox tiles and aligns facing toward adjacent thaumatorium blocks, `TileThaumatorium#getUpgrades()` restored and invoked on the 40-tick heat/update cadence with capacity shrink-pruning for `recipeHash`/`recipeEssentia`/`recipePlayer`), guarded by static contract tests `ThaumatoriumBrainboxUpgradeContractTest` and `BlockMetalDeviceBrainboxContractTest`, with passing `validate --smoke`.
Incremental Stage 9-d delta: alchemical smelting bonus baseline is now restored in `ConfigRecipes` (`initializeSmeltingBonusBaseline()` wired into init with ore-dictionary mappings for gold/iron/cinnabar/copper/tin/silver/lead, native cluster mappings, and raw meat/fish mappings to `itemNuggetEdible`), guarded by `ConfigRecipesSmeltingBonusCoverageTest`, with passing `validate --smoke`.
Incremental Stage 9-d delta: Thaumcraft alchemy object-tag baseline has been expanded in `ConfigAspects` (`registerThaumcraftAlchemyBaseline()` for shard/nugget/custom-ore/custom-plant/cosmetic/metal-device/essence/resource/zombie-brain tags used by crucible/alchemy flow), guarded by `ConfigAspectsAlchemyTagCoverageTest`, with passing `validate --smoke`.
Incremental Stage 9-d delta: crucible alchemy registration corpus has been expanded in `ConfigRecipes` (restored `BalancedShard_0..5`, `Alt*` duplication/manufacture/entropic recipes, `Pure*`/`Trans*` ore chains, `EtherealBloom`/`LiquidDeath`/`BottleTaint`, `CoreGather`, `BathSalts`, `SaneSoap`, plus taint-essence catalyst payload for `BottleTaint`), guarded by `ConfigRecipesCrucibleAlchemyCoverageTest`, with passing `validate --smoke`.
Incremental Stage 9-d delta: crucible/thaumatorium research-gate contracts are now statically guarded (`CrucibleResearchGateStaticGuardTest` enforces research-completion checks in `findMatchingCrucibleRecipe(...)` and `ContainerThaumatorium` programming plus specificity tie-break presence), with passing `validate`.
Incremental Stage 9-d delta: `ThaumcraftApi.getCraftingRecipeKey(...)` crucible-page lookup contracts are now statically guarded (`ThaumcraftApiCrucibleRecipeKeyStaticGuardTest` enforces the `CrucibleRecipe[]` page branch, key-cache write, and research-gated return path), with passing `validate`.
Incremental Stage 9-d runtime delta: dedicated `smoke-server` baseline rerun after latest alchemy/gating/brainbox/tag updates passes Forge ready-state and crash-marker sweep; manual crucible/thaumatorium progression scenarios remain open.
Incremental Stage 9-d delta: `TileCrucible.attemptSmelt` contracts are now statically guarded (`TileCrucibleSmeltContractStaticGuardTest` enforces thrower-NBT fallback, research matcher call path, fixed 50 mB drain on craft, and no-aspect reject early-return behavior), with passing `validate`.
Incremental Stage 8-e delta: client fallback FX proxy helpers (`blockSparkle`/`beam`/`bolt`/`burst`/`wispFX3`/`sparkle`/`particleCount`) are now implemented, and active FX packets `PacketFXVisDrain` + `PacketFXBlockArc` now schedule client handlers instead of no-op paths, guarded by `ClientProxyFxStaticGuardTest` and `PacketFXSerializationTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXBlockSparkle` now has reference-shaped payload serialization + client-scheduled proxy dispatch, and `ServerTickEventsFML` block-swap flow now sends this packet again (`sendToAllAround`) for replacement sparkle feedback; guarded by expanded `PacketFXSerializationTest` + `ClientProxyFxStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXShield` now has serialization + client-scheduled fallback handler routing, and `EventHandlerRunic` player/champion shield paths now send this packet again; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXSonic` now has serialization + client-scheduled fallback handler routing, and `EntityEldritchGuardian` sonic attack path now broadcasts this packet again; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXWispZap` now has serialization + client-scheduled fallback handler routing, and `EntityWisp` attack path now broadcasts this packet again in the reference attack window; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXZap` now has serialization + client-scheduled fallback handler routing, and `FocusShock.chainLightning` now broadcasts this packet again on each center→closest chain hop with the reference 64-block target-point radius; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXBlockZap` now has serialization + client-scheduled fallback handler routing (including client zap sound), and active tile send-sites are restored in `TileEldritchTrap.update` and `TileInfusionMatrix.inEvZap`; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXEssentiaSource` now has serialization + client-scheduled fallback handler routing (beam-style source stream), and `EssentiaHandler.drainEssentia` now broadcasts this packet again on successful aspect drain with the reference 32-block target-point radius; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXInfusionSource` now has serialization + client-scheduled fallback handler routing for both entity-target and pedestal-offset stream modes, and `TileInfusionMatrix` now broadcasts this packet again in XP-drain and ingredient-pull wind-up paths; guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXBlockDig` and `PacketFXBlockBubble` now have serialization + client-scheduled fallback handler routing (block/item dig particle paths plus bubble particle path), guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketFXBeamPulse` and `PacketFXBeamPulseGolemBoss` now have serialization + client-scheduled fallback handler routing through proxy beam paths (including dual-color golem-boss pulse fallback), guarded by expanded FX serialization/static tests with passing `validate --smoke`.
Incremental Stage 8-e delta: missing Stage 8-e misc FX texture atlases/overlays were copied from `thaumcraft_src/assets/thaumcraft/textures/misc/` into `src/main/resources/assets/thaumcraft/textures/misc/`, and `FxTextureAssetCoverageTest` now enforces that baseline asset set in non-GUI validation (`validate --smoke` passing).
Incremental Stage 8-e delta: `PacketBoreDig` now has serialization + client-scheduled handler routing, and `TileArcaneBore` now broadcasts/consumes packed dig replay events (`sendDigEvent`/`getDigEvent`) with fallback client crack-particle dig FX; guarded by new `PacketBoreDigSerializationTest` plus expanded static guard coverage with passing `validate --smoke`.
Incremental Stage 8-e delta: runic shield sound hooks are now restored in `EventHandlerRunic` for player heal/emergency recharge and champion/eldritch shield-hit reactions (`TCSounds.RUNICSHIELDEFFECT` / `TCSounds.RUNICSHIELDCHARGE`), guarded by expanded static coverage with passing `validate --smoke`.
Incremental Stage 8-e delta: `PacketMiscEvent` and `PacketWarpMessage` now have serialization + client-scheduled handler baselines, and `WarpEvents` now re-sends warp/mist/sticky-warp client signals (`PacketMiscEvent(0/1)` and `PacketWarpMessage(type=1, data=-1)`), guarded by new `PacketMiscWarpSerializationTest` plus expanded static coverage with passing `validate --smoke`.
Incremental Stage 8-e delta: `EntityEldritchGuardian` now restores its periodic short-mist client signal send (`PacketMiscEvent((short)2)`) for nearby players outside Outer Lands, guarded by expanded static coverage with passing `validate --smoke`.
Incremental Stage 8-e delta: `TileCrucible` client FX hooks are now restored (`drawEffects` + `receiveClientEvent` sparkle/boil routing), `CommonProxy`/`ClientProxy` now include fallback crucible FX helper methods (`crucibleFroth`, `crucibleFrothDown`, `crucibleBubble`, `crucibleBoilSound`, `crucibleBoil`), and static guards now lock both hook presence and placeholder removal (`TileCrucibleSmeltContractStaticGuardTest`, `ClientProxyFxStaticGuardTest`) with passing `validate --smoke`.
Incremental Stage 8-e delta: `EntityEldritchGuardian` now restores its client ambient wisp trail proxy hook (`Thaumcraft.proxy.wispFXEG(...)`) with reference-shaped jittered coordinates, `CommonProxy`/`ClientProxy` now expose fallback `wispFXEG(...)`, and static coverage now guards both client override presence and guardian call-site presence (`ClientProxyFxStaticGuardTest`) with passing `validate --smoke`.
Incremental Stage 8-e delta: `EntityFallingTaint` now restores its client landing FX proxy hook loop (`Thaumcraft.proxy.taintLandFX(this)` on landing/first-fall tick), `CommonProxy`/`ClientProxy` now expose fallback `taintLandFX(Entity)`, and static coverage now guards both client override presence and falling-taint call-site presence (`ClientProxyFxStaticGuardTest`) with passing `validate --smoke`.
Incremental Stage 8-e delta: `ServerTickEventsFML` block-swap paths now restore reference-shaped silk-harvest and fortune-drop handling (`canSilkHarvest` + `getDrops`) and restore the swap-queue wand sound cue (`world.playSound(..., TCSounds.WAND, SoundCategory.PLAYERS, 0.25f, 1.0f)`), with static guard coverage in `ClientProxyFxStaticGuardTest` and passing `validate --smoke`.
Incremental Stage 8-e delta: `TileSensor` note-event buffering hooks are now restored (`noteBlockEvents` weak-map baseline, `EventHandlerWorld` note capture + unload cleanup, `ServerTickEventsFML` per-tick buffer clear), guarded by new `TileSensorNoteEventStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `EventHandlerWorld.onItemCrafted(...)` now restores reference-shaped special crafting return hooks for `itemResource:13` (`ItemEssence` matrix returns) and `blockMetalDevice:3` (center-slot return), guarded by new `EventHandlerWorldCraftingReturnStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `EventHandlerEntity.onItemPickup(...)` now restores the reference fake-player pickup cancel guard for `FakeThaumcraft*` actors, guarded by new `EventHandlerEntityPickupStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `EventHandlerWorld.onHarvestDrops(...)` now restores reference-shaped special-mining replacement flow (tool/focus gate, fortune/treasure scaling, and `Utils.findSpecialMiningResult(...)` drop replacement), while `Utils` now restores `specialMiningResult`/`specialMiningChance` maps plus `addSpecialMiningResult(...)` and `findSpecialMiningResult(...)`; guarded by new `EventHandlerWorldHarvestDropsStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: `EventHandlerEntity.onEntityInteract(...)` now restores the reference golem owner-check cancel guard (`EntityGolemBase` + non-owner message/cancel path), guarded by new `EventHandlerEntityInteractStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-e delta: champion modifier `showFX(...)` hooks are now restored across the full TODO cluster (`ChampionModArmored/Bold/Fire/Grim/Infested/Mighty/Poison/Sickly/Spined/Undying/Vampire/Warded/Warp`) via proxy fallback routing, and `CommonProxy`/`ClientProxy` now expose `drawGenericParticles(...)` + `slimeJumpFX(...)` helper surface; guarded by new `ChampionModFxStaticGuardTest` plus expanded `ClientProxyFxStaticGuardTest`, with passing `validate --smoke`.
Incremental Stage 8-e delta: `ParticleEngine` is no longer an empty stub and now provides a client-safe queued dispatcher baseline (`addEffect(World, Particle)`, `pendingParticles` queue, `TickEvent.Phase.END` drain into `mc.effectRenderer.addEffect(...)`, dimension gate, per-tick cap, and render-world bookkeeping fields), with `ClientProxyFxStaticGuardTest` expanded to lock this non-stub engine contract; `validate --smoke` passing.
Incremental Stage 8-e delta: `FocusWarding` now restores ward/unward sparkle packet broadcasts (`PacketFXBlockSparkle`, color `0xFC9A00`, target-point radius `32.0D`) and `FocusPortableHole.createHole(...)` now restores the reference sparkle cue broadcast (`PacketFXBlockSparkle`, color `0x400040`, radius `32.0D`) after successful hole placement; `ClientProxyFxStaticGuardTest` now guards both focus send-path contracts; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalAxe` now restores non-stub utility/fx baseline (active-use hold contract, thaumium repair key `itemResource:2`, item-magnet pull loop with `Thaumcraft.proxy.crucibleBubble(...)`, and wood-log break bubble send path via `PacketFXBlockBubble` + `TargetPoint` radius `32.0D` + `TCSounds.BUBBLE`), guarded by new `ItemElementalAxeStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalPickaxe` now restores non-stub fire/use feedback baseline (`toolClasses`/`rarity`/thaumium-repair contracts, server-side `onLeftClickEntity` fire hook `setFire(2)`, and on-use `damageItem(5)` + `TCSounds.WANDFAIL` cue + client swing fallback), guarded by new `ItemElementalPickaxeStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalSword` now restores non-stub lift/chain-hit baseline (`RARE` + thaumium-repair contracts, active-use hold contract with `EnumAction.BLOCK` and `72000` duration, `onUsingTick` motion/fall-control + `Utils.resetFloatCounter(...)`, nearby push sweep, secondary-target chain-hit via `player.attackTargetEntityWithCurrentItem(...)`, and `WIND`/`SWING`/`SMOKE_NORMAL` feedback cues), guarded by new `ItemElementalSwordStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalHoe` now restores non-stub till/bonemeal/tree-growth baseline (enchantability `5`, `RARE` + thaumium-repair contracts, non-sneak `3x3` till sweep with `blockSparkle`, bonemeal fallback with durability/sparkle/sound cue, and `blockCustomPlant` meta `0/1` growth hooks with reference-shaped durability thresholds/costs), while `BlockCustomPlant.growGreatTree(...)`/`growSilverTree(...)` are now public to match the reference call surface; guarded by new `ItemElementalHoeStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalHoe.onItemUse(...)` no-op branch now aligns with reference boolean-false semantics by returning `PASS` (instead of `FAIL`) when the till/bonemeal/tree-growth flow does not apply, guarded by expanded `ItemElementalHoeStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalShovel` now restores non-stub architect/burst baseline (`IArchitect` surface + `shovel`/`RARE`/thaumium-repair contracts, orientation-aware `3x3` placement-copy sweep with inventory consume and sparkle feedback, break-side capture in `onBlockStartBreak`, and non-sneak `3x3` burst-mining gate in `onBlockDestroyed`), guarded by new `ItemElementalShovelStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalShovel.onItemUse(...)` client-side no-op branch now aligns with reference-style semantics by returning `PASS` (not `SUCCESS`) when `world.isRemote`, guarded by expanded `ItemElementalShovelStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalPickaxe` client-use path now restores the reference scan trigger baseline via side-safe proxy routing (`Thaumcraft.proxy.startScan(...)` + `CommonProxy` no-op stub + `ClientProxy` `RenderEventHandler.startScan(...)` dispatch), while `RenderEventHandler` now carries baseline scan state/lifecycle cleanup fields (`scanEntityId`/`scanPos`/`scanExpireAtMs`/`scanRange`); guarded by expanded `ItemElementalPickaxeStaticGuardTest` and `ClientProxyFxStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemElementalPickaxe.onLeftClickEntity(...)` now restores the reference pvp guard contract by suppressing player-target fire application when server PvP is disabled (while preserving non-player fire hits), guarded by expanded `ItemElementalPickaxeStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: thaumium equipment family contracts are now restored (`ItemThaumiumSword/Axe/Pickaxe/Shovel/Hoe`: `UNCOMMON` rarity, thaumium repair `itemResource:2`, toolclass restoration for axe/pickaxe/shovel, and hoe enchantability `5`), guarded by new `ItemThaumiumToolsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: void/crimson equipment family core contracts are now restored (`ItemVoidSword` rarity + pvp-gated debuff helpers + 60-tick wither hit, void tool rarity/toolclass/enchantability + pvp-gated 80-tick wither hit + shared self-repair, and `ItemCrimsonSword` `IWarpingGear` + rare rarity + dual-debuff hit + warp `2`), guarded by new `ItemVoidCrimsonToolsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemVoidSword`/`ItemCrimsonSword` now restore the reference special-sapping tooltip contracts (`enchantment.special.sapless` / `enchantment.special.sapgreat`) and `en_us.lang` now includes both keys, guarded by expanded `ItemVoidCrimsonToolsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemPrimalArrow` creative subtype baseline is now restored by expanding `getSubItems(...)` to all six primal variants (`meta 0..5`), guarded by new `ItemPrimalArrowStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemBowBone` core draw contracts are now aligned to reference baseline (`maxDamage=512`, `enchantability=3`, bone repair key, and early-release draw hook `ticks>18 -> stopActiveHand()`), guarded by new `ItemBowBoneStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: armor repair/rarity core contracts are now restored for `ItemThaumiumArmor` (`UNCOMMON` + thaumium repair `itemResource:2`) and `ItemGoggles` (`RARE` + leather repair), guarded by new `ItemArmorRepairRarityStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: cultist armor family core contracts are now restored (`ItemCultistRobeArmor` uncommon+leather repair+`IVisDiscountGear`/`IWarpingGear` 1%/warp1, `ItemCultistPlateArmor` uncommon+leather repair, `ItemCultistLeaderArmor` rare+leather repair, `ItemCultistBoots` uncommon+leather repair+warp1/vis1), guarded by new `ItemCultistArmorCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: robe/traveller armor core contracts are now restored (`ItemRobeArmor` uncommon + thaumic-cloth repair `itemResource:7`, and `ItemBootsTraveller` rare rarity), guarded by new `ItemRobeTravellerArmorCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemFortressArmor` core contracts are now restored (`ISpecialArmor`/`IGoggles`/`IRevealer` surface, `RARE` + thaumium repair `itemResource:2`, reference-shaped set+mask ratio bonus in `getProperties`, and `goggles`-gated revealer hooks), guarded by new `ItemFortressArmorCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: fortress-armor tooltip/lang contracts are now restored (`ItemFortressArmor.addInformation` goggles/mask NBT tooltip branches with reference keys plus `en_us.lang` keys `item.ItemGoggles.name` and `item.HelmetFortress.mask.0..2`), guarded by expanded `ItemFortressArmorCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: void armor family core contracts are now restored (`ItemVoidArmor` uncommon rarity and `ItemVoidRobeArmor` epic rarity + `IGoggles`/`IRevealer`/`ISpecialArmor` surface, vis-discount tooltip, helmet revealer gates, and mitigation hooks), guarded by new `ItemVoidArmorCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: boots-traveller hover movement core contracts are now restored in `Hover.doHover(...)` (forward/creative gate, client step-height boost+restore, on-ground bonus thrust, airborne `jumpMovementFactor` branch, fall-distance dampening), with `Hover.resetHover(...)` now restoring previous step-height state; guarded by expanded `ItemRobeTravellerArmorCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: hover-harness core contracts are now tightened (`ItemHoverHarness` GUI open path uses floored coordinates for negative-pos parity) and guarded by new `ItemHoverHarnessCoreContractsStaticGuardTest` (rarity/repair/vis-discount/onArmorTick/GUI-open contracts); `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemBucketPure` use-result contracts are now aligned to reference no-op semantics by returning `PASS` on no-hit/no-edit/no-place branches (instead of `FAIL`), while keeping creative/survival place-result split; guarded by new `ItemBucketPureCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemResearchNotes` core consumption/result contracts are now aligned to reference by restoring unconditional note consumption on successful completion and hidden-fail discovery paths, and by returning `PASS` on missing-requisites completion path; guarded by new `ItemResearchNotesCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemResearchNotes.revealDiscovery(...)` invalid-note fallback now aligns with reference non-failing use semantics by returning `PASS` (instead of `FAIL`) after the invalid-note message path, guarded by expanded `ItemResearchNotesCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemEldritchObject` metadata tooltip/lang contracts are now restored (`addInformation` branches for metas `0..4`, including `Creative Mode Only`) and `en_us.lang` now includes `item.ItemEldritchObject.text.1..6`, guarded by new `ItemEldritchObjectCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemEldritchObject` use-first side-result contracts are now aligned to reference semantics (pearl/obelisk paths return `SUCCESS` server-side and `PASS` client-side), guarded by expanded `ItemEldritchObjectCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemLootBag` post-use stack-consumption timing is now aligned to reference by applying `stack.shrink(1)` outside the server-only loot generation branch, guarded by new `ItemLootBagCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemManaBean` use-result semantics are now aligned to reference by returning `PASS` on invalid/no-op gates and `SUCCESS` when growth target is valid but occupied, while preserving server-side pod placement/aspect transfer logic; guarded by new `ItemManaBeanCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 5-coupled delta: `ItemEssence` client preview result semantics are now aligned to reference by returning `PASS` (not `SUCCESS`) in remote swing-only fill/empty branches, guarded by new `ItemEssenceCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `ClientProxy.setupEntityRenderers()` now registers client-side handlers for every `ConfigEntities.ENTITIES` entry via `RenderingRegistry`, using the new `RenderNoop` baseline renderer to eliminate empty-registration risk while full renderer/model parity remains open; guarded by new `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: missing Stage 8-d renderer asset corpus (`textures/models`, `textures/entity`, `textures/misc`, including `.obj`/`.mtl`/`.mcmeta`) is now copied from `thaumcraft_src/assets/thaumcraft/` into `src/main/resources/assets/thaumcraft/`, with new `EntityRendererAssetCoverageTest` guarding critical entity-renderer baseline paths under non-GUI validation (`validate --smoke` passing).
Incremental Stage 8-d delta: item-like entity rendering now uses non-noop `RenderEntityItem` registrations (`EntitySpecialItem`, `EntityPermanentItem`, `EntityFollowingItem`, `EntityItemGrate`) while remaining entities continue through fallback `RenderNoop`; static guard coverage was expanded in `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: projectile entity baseline rendering now uses non-noop `RenderSnowball` registrations (`EntityDart`, `EntityPrimalArrow`, `EntityBottleTaint`, `EntityAlumentum`, `EntityPrimalOrb`, `EntityFrostShard`, `EntityPechBlast`, `EntityEldritchOrb`, `EntityGolemOrb`, `EntityShockOrb`, `EntityExplosiveOrb`, `EntityEmber`, `EntityGolemBobber`) with `ConfigItems`-aware fallback binding for `itemPrimalArrow`/`itemBottleTaint`; guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: compatible zombie/spider groups now use non-noop vanilla fallback renderers (`RenderZombie`/`RenderSpider`) instead of `RenderNoop`, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: taint animal-like groups now use non-noop typed fallback rendering via new `RenderFallbackLiving` (`EntityTaintChicken`/`EntityTaintCow`/`EntityTaintPig`/`EntityTaintSheep`/`EntityTaintVillager`/`EntityTaintCreeper`) with TC model-texture paths, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` and passing `validate --smoke`.
Incremental Stage 8-d delta: cultist entity group now uses non-noop typed biped fallback rendering via new `RenderFallbackBiped` (`EntityCultistKnight`/`EntityCultistCleric`/`EntityCultistLeader`) with TC texture baseline, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` and passing `validate --smoke`.
Incremental Stage 8-d delta: remaining monster baseline now uses non-noop fallback renderer registrations (`EntityFireBat`/`EntityWisp`/`EntityPech`/`EntityEldritchGuardian`/`EntityEldritchWarden`/`EntityEldritchGolem`/`EntityEldritchCrab`/`EntityThaumicSlime`/`EntityTaintSpore`/`EntityTaintSporeSwarmer`/`EntityTaintSwarm`/`EntityTaintacle`/`EntityTaintacleSmall`/`EntityTaintacleGiant`) via `RenderFallbackLiving`/`RenderFallbackBiped` with existing TC texture baselines, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` and passing `validate --smoke`.
Incremental Stage 8-d delta: remaining special entity classes now also have non-noop baseline registrations (`EntityAspectOrb`/`EntityFallingTaint`/`EntityGolemBase`/`EntityTravelingTrunk`/`EntityCultistPortal`) via `RenderSnowball`/`RenderFallbackLiving`/`RenderFallbackBiped`, so the entire current `ConfigEntities.ENTITIES` set has explicit non-noop renderer coverage; guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: coverage hardening test `ClientProxyEntityRendererCoverageByConfigTest` now enforces that every `makeEntry(Entity*.class)` in `ConfigEntities` has explicit `registerEntityRenderer(Entity*.class, ...)` coverage in `ClientProxy`, preventing silent fallback-to-noop registration drift; `validate --smoke` passing.
Incremental Stage 8-c delta: existing magical leaves client model/resource baseline (`BlockMagicalLeaves#getRenderLayer -> CUTOUT_MIPPED`, `BlockMagicalLeavesItem#getMetadata -> damage & 1`, `blockmagicalleaves` blockstate/models, greatwood/silverwood leaf textures) is now protected by new `BlockMagicalLeavesRenderContractTest` with passing `validate --smoke`.
Incremental Stage 8-d delta: `EntityPech` renderer baseline now uses dedicated `RenderPech` registration (`EntityPech -> RenderPech::new`) with explicit `getPechType()` texture routing across `pech_forage`/`pech_thaum`/`pech_stalker`, and static guard coverage was expanded in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityFireBat` renderer baseline now uses dedicated `RenderFireBat` registration (`EntityFireBat -> RenderFireBat::new`) with explicit vampire texture routing plus devil/vampire scale and hanging transform baselines (`getIsVampire`/`getIsDevil`/`getIsBatHanging`), and static guard coverage was expanded in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTravelingTrunk` renderer baseline now uses dedicated `RenderTravelingTrunk` registration (`EntityTravelingTrunk -> RenderTravelingTrunk::new`) with explicit anger texture routing (`trunk.png` vs `trunkangry.png`) and new `EntityTravelingTrunk#getAnger()` renderer contract accessor, with expanded static guard coverage in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityGolemBase` renderer baseline now uses dedicated `RenderGolemBase` registration (`EntityGolemBase -> RenderGolemBase::new`) with explicit golem-type texture routing across `golem_straw`/`golem_wood`/`golem_tallow`/`golem_clay`/`golem_flesh`/`golem_stone`/`golem_iron`/`golem_thaumium`, with expanded static guard coverage in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityWisp` renderer baseline now uses dedicated `RenderWisp` registration (`EntityWisp -> RenderWisp::new`) with explicit aspect-tinted color routing from `Aspect.getAspect(entity.getWispType())` on `wispy.png` plus render-color reset to white, with expanded static guard coverage in `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: eldritch guardian/warden baseline now uses dedicated renderer registrations (`EntityEldritchGuardian -> RenderEldritchGuardian::new`, `EntityEldritchWarden -> RenderEldritchWarden::new`) with dedicated texture paths and warden-specific 1.5x render scale baseline, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityEldritchGolem` baseline now uses dedicated `RenderEldritchGolem` registration (`EntityEldritchGolem -> RenderEldritchGolem::new`) with dedicated texture path and reference-shaped 2.15x render scale baseline, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityEldritchCrab` baseline now uses dedicated `RenderEldritchCrab` registration (`EntityEldritchCrab -> RenderEldritchCrab::new`) with dedicated base texture plus dedicated overlay layer wiring (`crab.png` + `craboverlay.png`), guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityCultistPortal` baseline now uses dedicated `RenderCultistPortal` registration (`EntityCultistPortal -> RenderCultistPortal::new`) with dedicated portal texture contract (`textures/misc/cultist_portal.png`), guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityThaumicSlime` baseline now uses dedicated `RenderThaumicSlime` registration (`EntityThaumicSlime -> RenderThaumicSlime::new`) with dedicated texture routing, slime-size/squish scale callback, and translucent gel layer wiring, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: taint spore trio now uses dedicated renderer registrations (`EntityTaintSpore -> RenderTaintSpore::new`, `EntityTaintSporeSwarmer -> RenderTaintSporeSwarmer::new`, `EntityTaintSwarm -> RenderTaintSwarm::new`) with explicit taint-spore texture/scale contracts plus dedicated noop swarm baseline, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: taintacle trio now uses dedicated renderer registrations (`EntityTaintacle -> RenderTaintacle`, `EntityTaintacleSmall -> RenderTaintacle`, `EntityTaintacleGiant -> RenderTaintacle`) with shared taintacle texture contract and per-entity scale profiles (including giant 1.33x baseline), guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: brainy/inhabited zombie group now uses dedicated renderer registrations (`EntityBrainyZombie -> RenderBrainyZombie::new`, `EntityGiantBrainyZombie -> RenderBrainyZombie::new`, `EntityInhabitedZombie -> RenderInhabitedZombie::new`) with explicit thaumcraft zombie texture contracts (`bzombie.png` / `czombie.png`), guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: mind/taint spider pair now uses dedicated renderer registrations (`EntityMindSpider -> RenderMindSpider::new`, `EntityTaintSpider -> RenderTaintSpider::new`) with explicit taint-spider base texture, dedicated eyes-layer wiring, and width-driven scale baselines, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: cultist trio now uses dedicated renderer registrations (`EntityCultistKnight -> RenderCultist`, `EntityCultistCleric -> RenderCultist`, `EntityCultistLeader -> RenderCultist`) with shared `cultist.png` texture contract and per-entity shadow profiles, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: taint animal-like group now uses dedicated shared renderer registrations (`EntityTaintChicken`/`EntityTaintCow`/`EntityTaintPig`/`EntityTaintSheep`/`EntityTaintVillager`/`EntityTaintCreeper` -> `RenderTaintTextureLiving`) with explicit model-texture bindings per entity, guarded by updated `ClientProxyEntityRendererRegistrationStaticGuardTest` class/registration contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTaintCreeper` now uses dedicated `RenderTaintCreeper` registration with reference-shaped flash-intensity scale wobble and color-multiplier baselines (`getCreeperFlashIntensity`), and `EntityTaintCreeper` now exposes the flash-intensity accessor required by renderer timing interpolation; guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTaintVillager` now uses dedicated `RenderTaintVillager` registration with reference-shaped pre-render scale baseline (`0.9375f`) and villager texture contract, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTaintChicken` now uses dedicated `RenderTaintChicken` registration with reference-shaped wing-rotation callback (`field_756_e`/`field_752_b`/`field_757_d`/`destPos` interpolation) and dedicated chicken texture contract, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTaintCow` now uses dedicated `RenderTaintCow` registration with dedicated cow texture/model baseline (`textures/models/cow.png`, `ModelCow`, shadow `0.7f`), guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTaintPig` now uses dedicated `RenderTaintPig` registration with dedicated pig texture/model baseline (`textures/models/pig.png`, `ModelPig`, shadow `0.5f`), guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTaintSheep` now uses dedicated `RenderTaintSheep` registration with sheep base+fur texture layer baseline (`sheep.png`/`sheep_fur.png`) via dedicated `SheepFurLayer`, and `EntityTaintSheep` now exposes `getSheared()` accessor for renderer gating, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: obsolete shared fallback `RenderTaintTextureLiving` was removed after full taint animal-like migration to dedicated renderer classes, and static guard coverage now enforces absence of stale `ClientProxy` references to that renderer; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityTaintSheep` sheared-state contract baseline is now restored (`SHEEP_FLAGS` data parameter, `getSheared`/`setSheared`, shear-path state flip + wool drops, and `"Sheared"` NBT persistence), with static guard coverage expanded to enforce those contracts for `RenderTaintSheep` fur-layer gating; `validate --smoke` passing.
Incremental Stage 8-d delta: taint sheep renderer safety is hardened by replacing vanilla `ModelSheep1/2` with dedicated `ModelTaintSheep1/2` (no `EntitySheep` cast path) and by wiring taint sheep head-animation hooks (`sheepTimer`, `getHeadRotationPointY`, `getHeadRotationAngleX`), guarded by expanded static contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: taint sheep AI/timer baseline is now wired to renderer animation flow (`convertGrassAI` task + hostile target tasks + `updateAITasks` timer sync via `AIConvertGrass#getConvertTimer`), with static guard coverage expanded for task wiring and timer sync contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `RenderTaintCreeper` now includes a powered-only armor-charge overlay layer baseline (`CreeperArmorLayer` + `textures/entity/creeper/creeper_armor.png` + additive blend/texture scroll), and `EntityTaintCreeper` now exposes `getPowered()` for layer gating; static guard coverage now enforces the new layer/accessor contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `RenderTaintCreeper` armor texture now restores the reference namespace contract by using `new ResourceLocation("thaumcraft", "textures/entity/creeper/creeper_armor.png")` (instead of implicit vanilla namespace), with static guard coverage expanded for the explicit path contract; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` now restores baseline survivability contracts (`canBreatheUnderwater() -> true`, `canDespawn() -> false`) with static guard coverage pinning both hooks in the entity contract; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` NBT persistence now restores reference-shaped contracts (`Fuse -> fuseTime`, `ExplosionRadius` persistence, `"powered"` key) with backward-compatible read support for legacy `"Powered"` and static guard coverage for the key contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` now restores no-melee-hit attack behavior by overriding `attackEntityAsMob(Entity)` to return success without applying the default `EntityMob` damage path, with static guard coverage pinning the hook contract; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` ignite flow now restores reference-shaped primed sound + state-driven fuse progression (`timeSinceIgnited += state` with floor clamp) and baseline `1.5F` explosion power contract, with static guard coverage expanded for these contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` detonation path now restores a nearby non-tainted living poison splash baseline (`EntityLivingBase` radius query + `!ITaintedMob` filter + `Config.potionFluxTaint` application), with static guard coverage expanded for these side-effect contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` detonation path now also restores a taint biome/fibre spread baseline (`10` random attempts, `Utils.setBiomeAt(..., biomeTaint)` conversion, and replaceable-block fibre placement via `ConfigBlocks.blockTaintFibres`), with static guard coverage expanded for these spread contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` now restores reference-shaped `getMaxFallHeight()` combat/pathfinding hook (`3` idle, `3 + (int)(getHealth() - 1.0F)` in combat), with static guard coverage expanded for the formula contract; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` now restores fall-accelerated fuse behavior (`fall(...)` hook with `timeSinceIgnited += distance * 1.5F` and cap at `fuseTime - 5`), with static guard coverage expanded for the fall-fuse contract; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCreeper` now restores early client taint FX baseline in `onLivingUpdate()` (`ticksExisted < 5` loop via `Thaumcraft.proxy.particleCount(10)` + `taintLandFX(this)`), with static guard coverage expanded for these client-hook contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintChicken` now restores reference-shaped AI/fall/sound behavior contracts (constructor attack/target AI stack, underwater/no-despawn hooks, empty `fall(...)`, early client taint FX hook, and death-sound hurt baseline), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintCow` now restores reference-shaped AI/attribute/sound behavior contracts (constructor size/nav + attack/target AI stack, underwater hook, `MAX_HEALTH=40.0D`/`ATTACK_DAMAGE=6.0D`/`MOVEMENT_SPEED=0.27D`, early client taint FX hook, and death-sound hurt baseline), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintPig` now restores reference-shaped AI/attribute/sound/drop behavior contracts (constructor size/nav + attack/target AI stack, underwater/no-despawn hooks, `getMaxSpawnedInChunk=2`, `MAX_HEALTH=20.0D`/`ATTACK_DAMAGE=4.0D`/`MOVEMENT_SPEED=0.275D`, early client taint FX hook, ambient/hurt pig-ambient sound baseline, and `rand.nextInt(3) == 0` rare drop branch), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintSheep` now restores reference-shaped attribute/survivability/fx/sound/drop behavior contracts (`MAX_HEALTH=20.0D`/`ATTACK_DAMAGE=3.0D`/`MOVEMENT_SPEED=0.25D`, underwater/no-despawn hooks, `getTotalArmorValue=1`, early client `taintLandFX` loop, sheep-ambient sound baseline for ambient/hurt/death, and `rand.nextInt(3) == 0` rare drop branch), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintVillager` now restores reference-shaped AI/village/attribute/fx/drop behavior contracts (constructor path-nav + indoor/door/village movement stack, player melee/target hooks, `MAX_HEALTH=30.0D`/`ATTACK_DAMAGE=4.0D`/`MOVEMENT_SPEED=0.3D`, underwater/no-despawn hooks, village home refresh + aggressor update hooks, early client `taintLandFX` loop, and reference-shaped 11/12 + rare 18 drop conditions), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintSwarm` now restores reference-shaped summoned/flight/attack behavior contracts (`FLAGS` summoned bit + `damBonus` persistence, `setSize(2.0F,2.0F)`, `MAX_HEALTH=30.0D`/`ATTACK_DAMAGE=2.0D+damBonus`, fullbright + underwater/no-despawn hooks, taint-biome-constrained flight steering, summoned starvation self-damage, nearest-player targeting when unsummoned, nausea-on-hit + summoned recently-hit mark, and `"Flags"`/`"damBonus"` NBT with meta-11 drop branch), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintSporeSwarmer` now restores reference-shaped spawn-counter/swarm-burst behavior contracts (`spawnCounter=500`, swarmer `setSporeSize(10)` + fixed `1.0F` hitbox, `MAX_HEALTH=75.0D`/`ATTACK_DAMAGE=1.0D`, hurt-client sploosh hook, player-proximity burst trigger, status-id `6` sync/reset path, `TCSounds.ROOTS` ambient, and two-roll meta `11/12` drop branch), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintSpore` now restores reference-shaped fullbright/swarm-particle/burst behavior contracts (`swarm` particle lifecycle list, `getYOffset=0.0D`, render distance clamp `<4096.0D`, fullbright render hooks `15728880/1.0F`, `sporeOnUpdate` client `SPELL_MOB` buildup to `size/3`, and dedicated `sploosh(50)` burst helper path), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintacleSmall` now restores reference-shaped lifetime/attribute/no-drop behavior contracts (`lifetime=200`, constructor `0.22F/1.0F` + `XP=0`, `MAX_HEALTH=8.0D`/`ATTACK_DAMAGE=2.0D`, expiry self-damage `DamageSource.STARVE` `10.0F`, forced no-natural-spawn, and empty drop contract), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintacleGiant` now restores damageable-underwater survivability baseline by removing the erroneous always-invulnerable override and restoring explicit underwater breathing (`canBreatheUnderwater=true`) while preserving air-supply bypass (`decreaseAirSupply -> air`), with static guard coverage expanded to enforce the contract and prevent invulnerability regression; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintacle` now restores reference-shaped core spawn/target/combat contracts (substrate+biome spawn gating with nearby-taintacle exclusion, `getYOffset=0.25D`, non-tainted nearest-target acquisition, agitation-gated melee/range attack flow, enchantment-aware tentacle damage path, distance-triggered secondary `EntityTaintacleSmall` spawn hook, and eldritch-to-taint biome/fibre conversion path), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityGiantBrainyZombie` now restores reference-shaped anger/scale/drop behavior contracts (`ANGER` data-parameter sync, constructor XP/scale/leap-AI baseline, anger decay + dynamic resize, anger-driven attack scaling, anger-on-hit cap logic, `MAX_HEALTH=60.0D`/`ATTACK_DAMAGE=7.0D`, double 6-roll rotten-flesh drop waves, zombie-brain rare drop branch, and `"Anger"` NBT persistence), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityBrainyZombie` now restores reference-shaped target/reinforcement/drop behavior contracts (constructor `EntityAIHurtByTarget` priority wiring, `MAX_HEALTH=25.0D`/`ATTACK_DAMAGE=5.0D`, explicit `EntityZombie.SPAWN_REINFORCEMENTS_CHANCE=0.0D`, and 3-roll rotten-flesh + zombie-brain rare drop baseline), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `RenderMindSpider` now restores viewer-only render gating via `EntityMindSpider#getViewer()` + current-player name match in `doRender(...)`, with static guard coverage enforcing the viewer contract in both renderer and entity; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityMindSpider` now restores synced harmless/viewer contracts via `DataParameter` state (`HARMLESS`/`VIEWER`), `"harmless"`/`"viewer"` NBT persistence, harmless XP gating, and `spiderScaleAmount()` accessor; `RenderMindSpider` now consumes `spiderScaleAmount()` for reference-shaped scale callback, with static guard coverage expanded for the new entity/renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `RenderMindSpider` now restores a reference-shaped translucent base render pass (`Math.min(0.1F, ticksExisted / 100.0F)` alpha profile, depth-mask toggle, `SRC_ALPHA/ONE_MINUS_SRC_ALPHA` blend, and temporary alpha-threshold shift), with static guard coverage expanded for the alpha/depth contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityMindSpider` now restores harmless collision/attack behavior hooks (`getYOffset` harmless split, `canBeCollidedWith` true, `canTriggerWalking` false, and harmless gate in `attackEntityAsMob`), with static guard coverage expanded for the new behavior contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityMindSpider` attribute baseline now restores the reference combat contract by using `SharedMonsterAttributes.ATTACK_DAMAGE` (`1.0D`) instead of an incorrect movement-speed override, with static guard coverage pinning both presence of the attack attribute and absence of a movement-speed override; `validate --smoke` passing.
Incremental Stage 8-d delta: `RenderMindSpider` eyes layer now restores fullbright lightmap baseline (`61680` split + `OpenGlHelper.setLightmapTextureCoords`) while keeping additive eyes-pass blending contracts, with static guard coverage expanded for the new lightmap hook; `validate --smoke` passing.
Incremental Stage 8-d delta: `RenderTaintSpider` now restores reference-shaped scale/eyes contracts (`EntityTaintSpider#spiderScaleAmount()` + `Y*1.25` scale profile and fullbright eyes lightmap `61680` hook via `OpenGlHelper.setLightmapTextureCoords`), with static guard coverage expanded for renderer+entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityTaintSpider` now restores common-side baseline contracts required by reference entity/render parity (constructor size `0.4F/0.3F`, XP `2`, `MAX_HEALTH=5.0D`, `ATTACK_DAMAGE=2.0D`, `getYOffset=0.1D`, and 1-in-6 loot chance for resource 11/12 drops), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d / Stage 6-coupled delta: `EntityInhabitedZombie` now restores reference-shaped armor/spawn/no-drop contracts (`onInitialSpawn` cultist-armor chance profile `0.9F/0.6F`, explicit no-drop hook `getDropItem -> Item.getItemById(0)`, empty `onDeath(DamageSource)` suppression, and `onDeathUpdate` XP gate including `canDropLoot()` alongside crab-spawn path), with static guard coverage expanded for the entity contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityAspectOrb` now uses dedicated `RenderAspectOrb` registration (`EntityAspectOrb -> RenderAspectOrb::new`) instead of `RenderSnowball`, with reference-shaped particle-atlas billboard/tint/blend/age-scale baseline and expanded static guard coverage for both registration and renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: tightly-coupled projectile pair `EntityGolemOrb` + `EntityShockOrb` now uses shared dedicated `RenderElectricOrb` registrations (replacing both `RenderSnowball` fallbacks) with reference-shaped particle-atlas/additive-billboard/red-variant/bob-scale baseline, guarded by expanded `ClientProxyEntityRendererRegistrationStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityExplosiveOrb` now uses dedicated `RenderExplosiveOrb` registration (`EntityExplosiveOrb -> RenderExplosiveOrb::new`) instead of `RenderSnowball`, with reference-shaped particles2 billboard/frame-animation/0.7x scale baseline and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityEmber` now uses dedicated `RenderEmber` registration (`EntityEmber -> RenderEmber::new`) instead of `RenderSnowball`, with reference-shaped duration-driven particles billboard/scale baseline and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityEldritchOrb` now uses dedicated `RenderEldritchOrb` registration (`EntityEldritchOrb -> RenderEldritchOrb::new`) instead of `RenderSnowball`, with reference-shaped spike-burst + particle-billboard renderer baseline and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityPrimalOrb` now uses dedicated `RenderPrimalOrb` registration (`EntityPrimalOrb -> RenderPrimalOrb::new`) instead of `RenderSnowball`, with reference-shaped primal-color spike-burst + particle-billboard renderer baseline and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityPechBlast` now uses dedicated `RenderPechBlast` registration (`EntityPechBlast -> RenderPechBlast::new`) instead of `RenderSnowball`, with reference-shaped intentional no-op renderer baseline and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityAlumentum` now uses dedicated `RenderAlumentum` registration (`EntityAlumentum -> RenderAlumentum::new`) instead of `RenderSnowball`, with reference-shaped intentional no-op renderer baseline and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityDart` now uses dedicated `RenderDart` registration (`EntityDart -> RenderDart::new`) instead of `RenderSnowball`, with `RenderArrow<EntityDart>` + vanilla arrow texture baseline (`textures/entity/arrow.png`) and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityPrimalArrow` now uses dedicated `RenderPrimalArrow` registration (`EntityPrimalArrow -> RenderPrimalArrow::new`) instead of `RenderSnowball`, with typed arrow renderer baseline (`RenderArrow<EntityPrimalArrow>`, `textures/entity/arrow.png`) plus `arrowType`-driven color tint/reset surface (`BlockCustomOreItem.colors`), and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityGolemBobber` now uses dedicated `RenderGolemBobber` registration (`EntityGolemBobber -> RenderGolemBobber::new`) instead of `RenderSnowball`, with particles-atlas bobber quad baseline (`textures/particle/particles.png`) plus fisher tether line hooks (`entity.fisher`, `rightArm`-driven swing, interpolated body-yaw anchor, segmented sag line), and expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityFallingTaint` now uses dedicated `RenderFallingTaint` registration (`EntityFallingTaint -> RenderFallingTaint::new`) instead of `RenderSnowball`, with block-model atlas baseline (`TextureMap.LOCATION_BLOCKS_TEXTURE`, metadata-driven imitated state, world-state divergence gate, and `BlockRendererDispatcher`/`renderModel` path) plus expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.
Incremental Stage 8-d delta: `EntityFrostShard` now uses dedicated `RenderFrostShard` registration (`EntityFrostShard -> RenderFrostShard::new`) instead of `RenderSnowball`, with dedicated shard texture baseline (`textures/blocks/frostshard.png`), entity-id-seeded random/damage scale hooks, and explicit blend+cull cross-quad draw surface, plus expanded static guard coverage for registration + renderer contracts; `validate --smoke` passing.

Incremental Stage 7 delta: `WorldProviderOuter` now restores the reference null spawn-coordinate contract by replacing direct `getSpawnPoint()` override with `getSpawnCoordinate() -> null`, guarded by expanded `WorldProviderOuterStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 7 delta: `WorldProviderOuter` now restores the reference non-sky-colored dimension contract by overriding `isSkyColored()` to return `false`, guarded by expanded `WorldProviderOuterStaticGuardTest`; `validate --smoke` passing.
Latest Stage 9-a delta: `ConfigRecipes.registerSpecialRecipes(...)` now also registers `Clusters0..6` shapeless crystal recipes under stable `thaumcraft:clusters0..6` IDs, and `ConfigRecipes.init()` now reuses these registered handles for `ConfigResearch.recipes` keys `Clusters0..6` with fallback preservation, guarded by new `ConfigRecipesClusterRegistryStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8/9 boundary delta: client GUI routing contracts are now explicitly guarded by `ClientProxyGuiRoutingStaticGuardTest`, pinning `GUI_ARCANE_WORKBENCH` id continuity and `ClientProxy.getClientGuiElement(...)` routes for `GuiArcaneWorkbench`, `GuiResearchBrowser`, and `GuiFocalManipulator`; `validate --smoke` passing.
Incremental Stage 9-b delta: dynamic recipe lifecycle hardening now includes `ConfigRecipesDynamicArcaneRegistrationStaticGuardTest`, which enforces `ConfigRecipes.init()` anti-dup guards and registration path for `ArcaneWandRecipe`, `ArcaneSceptreRecipe`, and `InfusionRunicAugmentRecipe` in `ThaumcraftApi.getCraftingRecipes()`, with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes.registerSpecialRecipes(...)` now restores the hidden Triple Meat Treat ordinary recipe baseline as a registered shapeless Forge recipe (`thaumcraft:triplemeattreat`) using sugar + edible nuggets in the current 1.12 item model, guarded by new `ConfigRecipesTripleMeatTreatRegistryStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-b delta: arcane data coverage audit is now hardened by `ConfigRecipesArcaneRegistrationAuditStaticGuardTest`, which enforces uniqueness and reference-sized corpus (`89` unique `registerArcaneRecipe`/`registerShapelessArcaneRecipe` keys) plus representative key-family presence (`Banner_`, `PrimalArrow_`, tube shapeless variants, core progression keys); `validate --smoke` passing.
Incremental Stage 9-b delta: non-GUI verification hardening now includes static guard coverage for Arcane Workbench matcher/container flow (`ThaumcraftCraftingManagerArcaneMatcherStaticGuardTest`, `ContainerArcaneWorkbenchArcaneFlowStaticGuardTest`), pinning null-safe arcane matcher contracts, `IArcaneRecipe` filtering, workbench vis-cost probe path, and wand-slot/shift-click routing invariants; `validate --smoke` passing.
Incremental Stage 9-a delta: `ConfigAspects.registerOreDictionary()` now expands compat ore-dict aspect coverage from just ingots/ores to include `nugget*` and `dust*` tags for copper/tin/silver/lead when corresponding compat ingots are found, guarded by new `ConfigAspectsCompatOreDictDustNuggetCoverageTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigAspects.registerThaumcraftAlchemyBaseline()` now expands crafted-output tag coverage for key Stage 9-a progression items/blocks (table/thaumometer/inkwell/bauble blanks, thaumium+void armor/tools, and crafted cosmetic solid metas 4/5), guarded by new `ConfigAspectsStage9aCraftedOutputTagCoverageTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes.registerSpecialRecipes(...)` now restores the missing reverse tallow recipe baseline (`thaumcraft:blocktallow_decompose`, `BlockTallow -> itemResource x9 meta 4`), guarded by new `ConfigRecipesBlockTallowDecomposeStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes.registerSpecialRecipes(...)` now restores compat metal craft/smelting baseline for external `ingotCopper/Tin/Silver/Lead` entries by registering dynamic `compat_<metal>_nuggets_*` and `compat_<metal>_ingot` recipes plus native-nugget (`17..20`) smelting fallback to the first matching external ingot stack, guarded by new `ConfigRecipesCompatMetalRegistryStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes.registerSpecialRecipes(...)` now restores missing normal-crafting cosmetic/plant baseline recipes from reference (`quicksilverplant`, `sugarplant`, `cosmeticopaque0/1`, `cosmeticsolid1`, `resource6fromopaque0/1`) under stable `thaumcraft:*` ids, guarded by new `ConfigRecipesCosmeticPlantRegistryStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes.registerSpecialRecipes(...)` now restores missing normal-crafting baseline registrations for `BlockThaumium` (compress/decompress) and full thaumium/void equipment recipes (armor + tools) under stable `thaumcraft:*` ids, guarded by new `ConfigRecipesThaumiumVoidRegistryStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes.registerSpecialRecipes(...)` now restores the missing `JarLabel` ordinary-crafting baseline under Forge registry IDs (`thaumcraft:jarlabel`, per-aspect `thaumcraft:jarlabel_<aspectTag>` NBT-aware variants using `ShapelessNBTOreRecipe`, and `thaumcraft:jarlabelnull` clear recipe), guarded by new `ConfigRecipesJarLabelRegistryStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes.shapelessNBTOreRecipe(...)` now restores reference-shaped dedicated NBT-aware recipe construction via new `thaumcraft.common.lib.crafting.ShapelessNBTOreRecipe` (NBT+item+meta match semantics with ore-dictionary alternatives), replacing the plain `ShapelessOreRecipe` fallback; guarded by new `ConfigRecipesNbtShapelessStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes.registerSpecialRecipes(...)` now explicitly registers baseline normal recipes `MundaneAmulet`/`MundaneRing`/`MundaneBelt` plus `BlockFlesh`/`BlockTallow` under stable `thaumcraft:*` IDs, and `ConfigRecipes.init()` now prefers these registered handles for `ConfigResearch.recipes` entries (with fallback preservation), guarded by new `ConfigRecipesNormalRecipeRegistryStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: recipe-ID audit foundation is now hardened with `ConfigRecipesRegistryIdAuditStaticGuardTest`, which enforces `ConfigRecipes` `setRegistryName(...)` uniqueness plus strict `thaumcraft:*` namespace scope (including robe/void-robe dye custom IDs), with passing `validate --smoke`.
Incremental Stage 9-a delta: custom `IRecipe` IDs for robe/void-robe dye recipes are now normalized to stable `thaumcraft:*` namespaces in `ConfigRecipes.registerSpecialRecipes(...)` (replacing `forge:*` registrations), guarded by new `ConfigRecipesCustomRecipeIdStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigRecipes` now restores the missing furnace smelting output baseline by adding `initializeSmeltingBaseline()` (cinnabar/amber ore smelting, magical-log charcoal output, cluster iron/cinnabar/gold conversions, balanced-shard dust output, and coin-to-gold-nugget melt), wired before smelting-bonus registration in `ConfigRecipes.init()`, guarded by new `ConfigRecipesSmeltingBaselineStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `Config.initModCompatibility()` now restores a reference-shaped ore-dictionary compatibility scan baseline (compat metal/ore found-flags reset+population, special-mining nugget hooks for copper/tin/silver/lead ores, compat aspect-tag registration for key external ore names, and elemental-axe `woodLog` ore-list capture), and `Thaumcraft.postInit` now runs compat scan + `initWandComponents()` before recipes/aspects/research so compat-gated wand caps and recipe/aspect/research branches see populated flags; guarded by new `ConfigModCompatibilityLifecycleStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 9-a delta: `ConfigItems` now restores the reference ore-dictionary registration baseline via a dedicated `registerOreDictionary()` hook (custom ores/shards/resources, nugget/ingot aliases including thaumium/void, cluster aliases, and magical log/plank/sapling aliases), guarded by new `ConfigItemsOreDictionaryStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 6 delta: `EntityPrimalArrow` now restores reference launch/spawn-contract baselines by adding the coordinate constructor (`World, x, y, z`) and typed launch constructor (`World, shooter, velocity, type`) and by expanding `IEntityAdditionalSpawnData` payload to include motion, rotation, arrow type, and shooter entity id reconstruction, guarded by new `EntityPrimalArrowCoreContractsStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 6 delta: `EntityPechBlast` now restores reference launch-shape core contracts by adding the coordinate payload constructor (`World, x, y, z, strength, duration, nightshade`), and `FocusPech` now restores the throwable launch-speed baseline via explicit `blast.shoot(player, player.rotationPitch, player.rotationYaw, 0.0F, 1.5F, 1.0F)` under Forge 1.12 constructor semantics, guarded by new `EntityPechBlastCoreContractsStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 6 delta: `EntityCultist` no longer overrides ambient/hurt/death sounds with explicit `null` (restoring base hostile sound inheritance consistent with reference class shape), `EntityCultistSoundContractTest` now guards this contract, `TCSoundsStaticCoverageTest` enforces static key/resource consistency across `TCSounds`, `sounds.json`, and bundled `.ogg` assets, and `MonsterSoundNullOverrideAllowlistTest` now prevents new non-allowlisted `null` sound overrides in Stage 6 monster/boss classes (`test` + `validate` passing).
Incremental Stage 6 delta: `EntityCultist` now persists and restores home position with reference-style `HomeD/HomeX/HomeY/HomeZ` semantics and restores cultist faction targeting contracts (`isOnSameTeam` + `canAttackClass`), guarded by `EntityCultistBehaviorContractTest` with passing `test` + `validate --smoke`.
Incremental Stage 6 delta: `EntityCultistPortalLootPlacementContractTest` now guards stage-0 reward placement against regressions to vanilla chest placeholders, and `Stage6 GAP-7` text is aligned with the current `BlockLoot`/`blockLootCrate` implementation baseline.
Incremental Stage 6 delta: `MonsterSoundConstantCoverageTest` now statically scans `thaumcraft.common.entities.monster` for `TCSounds.*` references and enforces that each referenced sound constant is declared in `TCSounds`, hardening monster/boss sound-link integrity (`test` + `validate` passing).
Incremental Stage 5 delta: `ItemResonator.onItemUseFirst(...)` now restores reference-style tube-face diagnostics via `RayTracer.retraceBlock(...)` + `subHit` face override and restores localized untyped suction fallback (`tc.resonator3`) instead of a hardcoded string, guarded by new `ItemResonatorStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 5 delta: `ItemSanitySoap` now restores reference-shaped completed-use consumption semantics by removing the creative-mode bypass (soap is consumed after successful >195 tick cleanse in all modes), guarded by new `ItemSanitySoapStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 8/9 boundary delta: server GUI routing contracts are now explicitly guarded by `CommonProxyServerGuiRoutingStaticGuardTest`, pinning `GUI_ARCANE_WORKBENCH`/`GUI_THAUMONOMICON`/`GUI_FOCAL_MANIPULATOR` id continuity and `CommonProxy.getServerGuiElement(...)` routes for `ContainerArcaneWorkbench`, `ContainerFocalManipulator`, and the Thaumonomicon null branch; `validate --smoke` passing.
Incremental Stage 8/9 boundary delta: `ClientProxy.getClientGuiElement(...)` now restores the reference-shaped `WorldClient` gate before switch routing, guarded by expanded `ClientProxyGuiRoutingStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8/9 boundary delta: utility GUI routing contracts are now explicitly guarded by `ProxyUtilityGuiRoutingStaticGuardTest`, pinning focus-pouch/hand-mirror/hover-harness direct routes plus `magic box` (`IInventory`) and `spa` tile routes across both `CommonProxy.getServerGuiElement(...)` and `ClientProxy.getClientGuiElement(...)`; `validate --smoke` passing.
Incremental Stage 8/9 boundary delta: core GUI routing contracts are now explicitly guarded by `ProxyCoreGuiRoutingStaticGuardTest`, pinning golem/pech/traveling-trunk entity routes and thaumatorium/deconstruction-table/alchemy-furnace/research-table/arcane-bore tile routes across both server containers and client GUI screens; `validate --smoke` passing.
Incremental Stage 8-a / Stage 3 sync-contract delta: packet discriminator-table contracts are now explicitly guarded by `PacketHandlerRegistrationStaticGuardTest`, pinning lowercase channel identity, `REFERENCE_PACKET_COUNT` mismatch guard, and the full reference registration order/sides for all 39 packet entries (including dual-side `PacketNote` and FX tail ordering); `validate --smoke` passing.
Incremental Stage 4/8-a sync-contract delta: note-sensor networking and redstone baseline was restored by porting reference-shaped `PacketNote` payload/handler logic and `TileSensor` state/update flow (`note`/`tone`/`redstoneSignal`, note-block event matching, tone mapping, block-event note trigger), plus `BlockWoodenDevice` sensor hooks (pitch/tone interaction, neighbor tone refresh, redstone output/connectivity, and note sound/particle event playback); guarded by new `PacketNoteStaticGuardTest`, `TileSensorStaticGuardTest`, and `BlockWoodenDeviceSensorStaticGuardTest` with passing `validate --smoke`.
Incremental Stage 4 common-device delta: `BlockWoodenDevice` now restores reference-shaped arcane pressure-plate baseline (owner/access-controlled setting cycle on right-click, collision/update-tick state machine for meta `2/3`, entity-filtered trigger logic, pressed/unpressed metadata transitions with neighbor notifications, and reschedule loop), with static guard coverage expanded in `BlockWoodenDeviceSensorStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 4 common-device delta: `BlockWoodenDevice` now restores reference side-solid and creative-subtype contracts for wood-device metas used by bore/plank progression (`isSideSolid=true` for metas `4/6/7` and creative listing for metas `6`/`7`), guarded by expanded `BlockWoodenDeviceSensorStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 4/5 vis-network delta: `TileMagicWorkbenchCharger` now restores the reference inheritance and charge loop baseline by extending `TileVisRelay`, keeping the enlarged render AABB, and re-adding server tick wand charging from the workbench slot (`slot 10`, primal room scan, `drain<=5`, `consumeVis` to `addRealVis`), guarded by new `TileMagicWorkbenchChargerStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 4 common-device delta: `TileArcaneLamp` now restores reference-shaped server light-placement and cleanup baseline (orientation NBT, random airy placement with light-level gate, and 31x31x31 `removeLights` sweep for airy meta `3`), and `BlockMetalDevice` now restores coupled lamp hooks (cleanup on break and support check in `neighborChanged` for meta `7`), guarded by new `TileArcaneLampStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 4 common-device delta: `TileGrate` now restores the reference single-slot sided-input behavior (`ISidedInventory` open only on meta `5` from `UP`) and item routing by spawning `EntityItemGrate` with downward motion on server insertion; `EntityItemGrate` now includes the coordinate+stack constructor required by this path, guarded by new `TileGrateStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 4 common-device delta: `TileFluxScrubber` now restores reference-shaped server scrubber baseline (`charges/power/essentia` tick flow, AIR vis drain via `VisNetHandler`, 16-step shuffled flux scan/cleanup with sparkle FX, and `IEssentiaTransport` + NBT contracts keyed by `facing/charges/power/essentia`), guarded by new `TileFluxScrubberStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 4 common-device delta: `TileArcaneLampGrowth` now restores reference-shaped server growth-lamp baseline (`reserve/charges` tick flow, vis-essentia draw cadence and suction contracts, shuffled plant scan with `CropUtils` growth/blacklist gates, and per-target scheduled growth updates), with coupled support checks in `BlockMetalDevice` for meta `8` and lamp blacklist helpers in `CropUtils`; guarded by new `TileArcaneLampGrowthStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 4 relic-device delta: `TileMirror` now restores reference-shaped server mirror baseline (link restore/validation cadence, linked-output queue ejection, instability decay + ORDER vis damping, facing-based spawn vector/motion, and inventory sink forwarding to linked mirrors), while `BlockMirror` restores linked-drop metadata/NBT preservation, link invalidation for both mirror tile variants, item-collision transport routing, and support-neighbor break checks; guarded by new `TileMirrorStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 4 relic-device delta: `BlockMirrorItem` now restores reference-shaped link-item flow for both mirror variants (normal mirror meta `1` and essentia mirror meta `7`, each with `linkX/Y/Z`, `linkDim`, and `dimname` tags), and linked placement restore now applies to both `TileMirror` and `TileMirrorEssentia`; guarded by new `BlockMirrorItemStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 6-coupled delta: `EntityEldritchWarden.onUpdate()` now restores the missing client-side `Thaumcraft.proxy.wispFXEG(...)` trail baseline (reference jitter `±0.2` and `y + 0.25 * height`), and `ClientProxyFxStaticGuardTest` now guards both guardian and warden FX hooks; `validate --smoke` passing.
Incremental Stage 6 / Stage 8-e coupled delta: eldritch guardian and warden constructor pathing now restore reference swim-navigation baseline (PathNavigateGround-gated `setCanSwim(true)` in 1.12.2), and `EntityEldritchWarden.onUpdate()` spawn-state signal now matches reference by removing the extra server-only gate; guarded by expanded `EntityEldritchGuardianStaticGuardTest` and new `EntityEldritchWardenStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 3/9 boundary delta: server-side admin command baseline is now restored by porting `CommandThaumcraft` (`research`/`aspect`/`warp` subcommands with alias surface `thaumcraft`/`thaum`/`tc`, knowledge updates, and sync packets), and `Thaumcraft.serverLoad(...)` now registers the command instead of deferring; guarded by new `CommandThaumcraftStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 9-e validation-hardening delta: `ConfigResearchStaticGraphTest` now correctly treats only `.setVirtual()` entries as virtual (instead of suppressing lang checks for all entries) and now enforces reference-sized research corpus coverage (`201` `ResearchItem` registrations plus non-empty coverage for all six categories); `validate` passing.
Incremental Stage 9-e validation-hardening delta: `ConfigResearchStaticGraphTest` now also enforces absence of parent-cycle regressions across `setParents(...)` and `setParentsHidden(...)` links, closing another static-checklist item for impossible research graph loops; `validate` passing.
Incremental Stage 6 / Stage 8-e coupled delta: champion-mod baseline is now partially restored by porting `EntityUtils.CHAMPION_MOD` + reference-shaped `makeChampion(...)` modifier wiring (including warded/bold/mighty branches) and by re-enabling `EventHandlerRunic` champion defensive/offensive effect hooks via `ChampionModifier.mods[t].effect.performEffect(...)`; guarded by new `ChampionModRunicStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 6 delta: `EventHandlerEntity` now restores champion spawn-roll and lifecycle hooks (`onEntityJoinWorld` mob path with whitelist+tier roll, biome/Outer/danger modifiers, `EntityUtils.CHAMPION_MOD` base `-2` initialization, and non-champion `ATTRIBUTE_MOD_NONE` tagging), restores passive champion tick effects for modifier type `0`, and restores champion kill rewards (bonus XP split + lootbag drop), guarded by new `EventHandlerEntityChampionStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 delta: `EventHandlerEntity.onLivingJump` now restores the traveler-boots jump boost baseline (`itemBootsTraveller` armor-slot check with `motionY += 0.275D`), guarded by new `EventHandlerEntityTravellerJumpStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 delta: `EventHandlerEntity.onLivingUpdate` now restores traveler-boots haste movement baseline via `applyTravellerHasteMovement(...)` (flying guard, feet-slot `itemBootsTraveller` gate, `enchHaste` level scaling, and ground/water damping before `moveRelative`), guarded by new `EventHandlerEntityTravellerHasteStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 delta: `EventHandlerEntity.onLivingUpdate` now also restores hover-flight boundary enforcement (`enforceHoverFlightBounds(...)`) by disabling hover/flying every second in Outer Lands for non-creative players and by forcing hover-off when no hover harness is equipped, with restored `tc.break.fly` localization key in `en_us.lang`; guarded by new `EventHandlerEntityHoverBoundsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 6 delta: `EventHandlerEntity.onLivingDrops` now restores reference-shaped zombie/villager brain-drop contracts (normal zombies only with `recentlyHit` + looting-gated `rand.nextInt(10)-looting<1`, and villager looting-gated brain-fragment drop `itemResource` meta `18`), while removing the prior over-generous fallback path; guarded by new `EventHandlerEntityDropContractStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 delta: `EventHandlerEntity` item event hooks now restore reference contracts by writing tossed-item thrower NBT (`"thrower"`) in `onItemToss`, and by converting source water to `blockFluidPure` when `ItemBathSalts` expires in-world; guarded by new `EventHandlerEntityItemEventStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 delta: `EventHandlerEntity.onEntityJoinWorld` now restores warded-protection behavior for ender pearls by scanning nearby tiles for `TileOwned`, cancelling the pearl when found, and notifying the thrower with the original warning text; guarded by new `EventHandlerEntityEnderPearlStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 delta: `EventHandlerEntity.onPlayerBreakSpeed` now restores reference-shaped hover mining contract by applying `originalSpeed * 5.0F` only when the player is airborne and hover mode is active, replacing the prior focus-based slow-speed fallback; guarded by new `EventHandlerEntityBreakSpeedStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5 delta: `EventHandlerEntity.onItemUseFinish` now restores `Unnatural Hunger` cure/feedback behavior (rotten-flesh or zombie-brain downgrade/removal path with reduced potion reapply and normal-food warning path), while keeping warp-add behavior for warped foods; added missing `warp.text.hunger.1/2` keys to `en_us.lang`; guarded by new `EventHandlerEntityUnhungerStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 6 delta: `EventHandlerEntity` bow hooks now restore primal-arrow launch contracts (`ArrowNockEvent` active-hand action when primal arrows are present, and `ArrowLooseEvent` custom `EntityPrimalArrow` spawn with bow-type velocity profile, enchantment modifiers, durability/sound, and inventory consumption semantics); guarded by new `EventHandlerEntityPrimalArrowStaticGuardTest`; `validate --smoke` passing.
Incremental Phase 10 polish delta: removed stale no-op placeholder hook (`AttackEntityEvent` “Future: Pech left-click trade detection”) from `EventHandlerEntity` to align with reference class surface and eliminate remaining in-scope placeholder comments in `thaumcraft.common`; `validate --smoke` passing.
Incremental Stage 5 delta: `ItemEldritchObject` now restores reference-shaped metadata naming contract by switching subtype translation lookup back to meta-indexed keys (`item.thaumcraft.eldritch_object.0..4.name`) and by restoring the missing subtype localization entries (Eldritch Eye / Crimson Rites / Runed Tablet / Primordial Pearl / Eldritch Obelisk Placer), guarded by new `ItemEldritchObjectCoreContractsStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 6 delta: `EntityGolemBase.onLivingUpdate()` now restores the reference client bootup sound lifecycle (`bootup` decay `*= bootup / 33.1f` and `TCSounds.CAMERATICKS` playback with scaled volume/pitch while `core > -1`), guarded by new `EntityGolemBaseBootupStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-e / Stage 6-coupled delta: eldritch boss FX send-site parity is expanded by restoring `EntityEldritchWarden` packet broadcasts for screech/frenzy (`PacketFXSonic` on sonic branch, plus `PacketFXBlockArc`/`PacketFXBlockSparkle` ring effects during field frenzy) and by restoring `TileEldritchLock` airy-cleanup sparkle broadcasts (`PacketFXBlockSparkle` with reference color `0x400040`), guarded by expanded `EntityEldritchWardenStaticGuardTest` and `TileEldritchTilesStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5/9 boundary delta: wand-trigger wiring is restored in the post-init recipe path by reintroducing reference `WandTriggerRegistry.registerWandBlockTrigger(...)` registrations in `ConfigRecipes.init()`, reinstating `CommonProxy.wandManager`, and porting `WandManager` to `IWandTriggerManager` with reference dispatch gates plus working core transforms for events `0/1/5` (`Thaumonomicon`, `Crucible`, `Thaumatorium`) including sparkle packet fanout; guarded by new `WandManagerTriggerStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5/9 boundary delta: `WandManager` now restores reference-shaped Arcane Furnace wand-trigger transform baseline for event `2` (`INFERNALFURNACE` gate): multiblock fit scan (`obsidian`/`netherrack`/single cross-slot `nether_brick_fence`/lava core), vis-cost consume (`FIRE 50`, `EARTH 50`), and full 3x3x3 replacement into `blockArcaneFurnace` metas (`0/1..10`) with update notifications and wand sound; guarded by expanded `WandManagerTriggerStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5/9 boundary delta: `WandManager` now restores reference-shaped Infusion Altar wand-trigger transform baseline for event `3` (`INFUSION` gate): 3x3x3 fit scan against arcane/pedestal blueprint, primal vis-cost consume (`25` each), and replacement flow that converts corner pedestals + infusion pillars, writes pillar orientation (`2..5`), and activates the infusion matrix (`active = true`) with block updates and wand sound; guarded by expanded `WandManagerTriggerStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5/9 boundary delta: `WandManager` now restores reference-shaped Node Jar wand-trigger transform baseline for event `4` (`NODEJAR` gate): 3x3x4 fit scan (`slabWood` ring + glass shell + center non-jar `INode`), primal vis-cost consume (`70` each), and replacement flow that clears shell blocks, converts center to `blockJar` node variant (meta `2`), and transfers node payload (`aspects`, `type`, `modifier`, `id`) with reference modifier-degrade chance; guarded by expanded `WandManagerTriggerStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 5/9 boundary delta: `WandManager` now restores non-stub Oculus trigger routing for event `6` (`OCULUS` gate) by delegating to the already ported `TileEldritchAltar.onWandRightClick(...)` path with explicit altar+dark-node guards, removing the dead trigger branch while preserving server-side altar logic ownership; guarded by expanded `WandManagerTriggerStaticGuardTest`; `validate --smoke` passing.
Incremental Stage 8-c delta: TESR baseline wiring burst is now in place by binding core tile special renderers in `ClientProxy.setupTileRenderers(...)` (`TileJar*`, `TileNode`, `TileCrucible`, `TileAlembic`, pedestal pair, `TileInfusionMatrix`, `TileThaumatorium`, arcane bore pair, and `TileFocalManipulator`) and adding dedicated renderer classes under `thaumcraft.client.renderers.tile` (`TileRenderHelper`, `TilePedestalRenderer`, `TileCrucibleRenderer`, `TileJarRenderer`, `TileAlembicRenderer`, `TileNodeRenderer`, `TileRunicMatrixRenderer`, `TileThaumatoriumRenderer`, `TileFocalManipulatorRenderer`, `TileArcaneBoreRenderer`, `TileArcaneBoreBaseRenderer`); guarded by new `ClientProxyTileRendererBurstCoverageTest` plus existing `ClientProxyFxStaticGuardTest`, with passing `compileJava`, targeted tests, and `validate --smoke` (including `smoke-server` ready-state pass); GUI/manual render verification remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-e delta: dedicated client beam/bolt baseline is now in place by adding `thaumcraft.client.fx.beams.FXBeam`, `thaumcraft.client.fx.beams.FXBeamGolemBoss`, and `thaumcraft.client.fx.bolt.FXLightningBolt`, and by wiring `ClientProxy.beam(...)` / `ClientProxy.bolt(...)` to enqueue these effects through `ParticleEngine.addEffect(...)` while preserving fallback particle paths; guarded by new `ClientProxyDedicatedBeamBoltStaticGuardTest` plus existing `ClientProxyFxStaticGuardTest`, with passing targeted FX tests and `validate --smoke` (`compileJava`, `test` `221/221`, `jar`, `check-jar`, `smoke-server` ready-state pass); GUI/manual visual parity remains `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 9 recipe-system delta: restored missing reference `ConfigResearch.recipes` handles for special normal-recipe family and compound wand-trigger blueprints by wiring `specialResearchRecipeHandles` + `recipeJarLabelAspects` map/list flow in `ConfigRecipes` (keys now include `InfusionAltar`, `NodeJar`, `JarLabel`, `JarLabelNull`, `BlockThaumium`, full `Thaumium*` and `Void*` armor/tool handles, plus `JarLabel0..n` dynamic handles), and by extending Stage 9 compound map payload with reference-shaped `InfusionAltar` and `NodeJar` blueprint arrays; guarded by new aggregate `ConfigRecipesResearchHandleParityStaticGuardTest`, with passing focused config tests and `validate --smoke` (`compileJava`, `test` `222/222`, `jar`, `check-jar`, `smoke-server` ready-state pass). Remaining documented reference gap in this cluster: `TallowCandle` key stays deferred because `blockCandle` content is currently absent in this port branch.
Incremental Stage 9 recipe/content delta: closed the remaining `TallowCandle` handle deferral by porting baseline candle content (`BlockCandle` block + metadata item registration + client model/color wiring + copied candle assets/localization), registering normal-crafting `tallowcandle` plus reference-shaped recolor (`tallowcandle_dye_1..15`) and reset (`tallowcandle_reset`) recipes, and wiring `TallowCandle` into `specialResearchRecipeHandles` so `ConfigResearch.recipes` receives the concrete handle; guarded by updated `ConfigRecipesResearchHandleParityStaticGuardTest`, with passing focused test and `validate --smoke` (`compileJava`, `test` `222/222`, `jar`, `check-jar`, `smoke-server` ready-state pass).
Incremental Stage 9-e validation-hardening delta: `ConfigResearch.recipes` now uses a strict init-time lookup mode (`ResearchRecipeHandleMap` + `strictRecipeLookups` gate) so both missing recipe-handle keys and `key->null` handles fail fast with explicit `IllegalStateException` during `ConfigResearch.init()` instead of producing silent null recipe pages; strict mode is disabled after init to preserve non-init lookup behavior. Added aggregate guard `ConfigResearchStrictRecipeLookupStaticGuardTest`; passing focused research tests and `validate --smoke` (`compileJava`, `test` `223/223`, `jar`, `check-jar`, `smoke-server` ready-state pass).
Incremental Stage 9 recipe-system hardening delta: removed legacy `TallowCandle -> null` fallback insertion from `ConfigRecipes.init()` so recipe-handle failures are no longer masked by placeholder null entries, aligning with strict Stage 9-e init-time recipe-handle validation and ensuring missing registrations fail explicitly during research graph build; passing focused config/research tests and `validate --smoke` (`compileJava`, `test` `223/223`, `jar`, `check-jar`, `smoke-server` ready-state pass).
Incremental Stage 9-e validation-hardening delta: `ConfigResearch` page wiring now routes all recipe handle lookups through typed helpers (`recipeI`/`recipeArcane`/`recipeCrucible`/`recipeInfusion`/`recipeInfusionEnchantment`/`recipeList`) backed by shared `requireRecipeHandle(...)` runtime validation, so research graph registration now fails explicitly on key-missing, null, and wrong-handle-type mismatches instead of deferred `ClassCastException`/null page failures; updated aggregate guards (`ConfigResearchStrictRecipeLookupStaticGuardTest`, `ConfigResearchRecipeKeyCoverageTest`) and passing focused research tests plus `validate --smoke` (`compileJava`, `test` `223/223`, `jar`, `check-jar`, `smoke-server` ready-state pass).
Incremental Stage 8-e FX delta: added dedicated client-side shield/sonic particle emitters (`FXShieldRunes`, `FXSonic`) and wired new proxy surface (`CommonProxy` no-op stubs + `ClientProxy` `sonicFX`/`shieldRunesFX` overrides) so `PacketFXShield`/`PacketFXSonic` now trigger dedicated effects through `ParticleEngine` in addition to existing burst/bolt fallback cues; updated aggregate FX guard coverage in `ClientProxyFxStaticGuardTest`, with passing focused FX tests and `validate --smoke` (`compileJava`, `test` `223/223`, `jar`, `check-jar` summary now `7269`/`1248`, `smoke-server` ready-state pass).
Incremental Stage 9-e research-page coverage delta: restored the missing reference recipe-page lookup coverage in `ConfigResearch` (272 unique lookup keys, now matching decompiled 1.7.10 `ConfigResearch`) by wiring recipe-backed pages for alchemy and progression branches that were still text-only (`ALCHEMICALDUPLICATION`/`ALCHEMICALMANUFACTURE`/`ENTROPICPROCESSING`, `LIQUIDDEATH`/`BOTTLETAINT`, `PURE*`/`TRANS*`, `ETHEREALBLOOM`, `BATHSALTS`/`SANESOAP`, `TALLOW` candle page, `JARLABEL` direct + dynamic `JarLabel0..n` + null-reset pages), plus `INFUSION` altar layout page, `NODEJAR` multiblock page, `GOLEMTHAUMIUM` thaumium-block page, `COREGATHER` crucible page, and `VOIDMETAL` void tool/armor recipe pages; added `buildJarLabelAspectPages()` helper and expanded `ConfigResearchRecipeKeyCoverageTest` to enforce reference-sized lookup coverage and include `specialResearchRecipeHandles` keys. Validation: focused `ConfigResearch` tests passing and `validate --smoke` PASS (`compileJava`, `test` `223/223`, `jar`, `check-jar` summary `7269`/`1248`, `smoke-server` ready-state pass). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 9-e strict-lookup hardening delta: `buildJarLabelAspectPages()` now mirrors reference semantics and strict-init guarantees by iterating `Aspect.aspects.values().size()` and requiring every indexed `JarLabelN` handle through `recipeI(...)`, so missing dynamic jar-label recipe handles can no longer be silently skipped by prefix-probe iteration; this keeps `ConfigResearch` fail-fast behavior intact for jar-label page arrays under `strictRecipeLookups`. Validation: focused `ConfigResearch` tests PASS and `validate` PASS (`compileJava`, `test` `223/223`, `jar`, `check-jar` summary `7269`/`1248`).
Incremental Stage 8-b/9-e client-content delta: restored reference research-note/discovery item visual contracts by splitting `itemResearchNotes` model routing in `ClientProxy.setupItemRenderers(...)` (`0..63` note model, `64..127` discovery model), adding discovery-layer model asset (`models/item/discovery.json`), and reintroducing overlay tint behavior in `ItemResearchNotes.getColorFromItemStack(...)` with note-data color fallback (`0x999999`) plus client color-handler registration. Guard coverage expanded via `ItemResearchNotesCoreContractsStaticGuardTest` and new `ClientProxyResearchNotesModelStaticGuardTest`; validation: focused tests PASS and `validate --smoke` PASS (`compileJava`, `test`, `jar`, `check-jar`, `smoke-server` ready-state pass). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c TESR burst delta: expanded node-device renderer coverage by adding dedicated client TESRs for `TileNodeStabilizer`, `TileNodeConverter`, and `TileVisRelay`, and by extending `ClientProxy.setupTileRenderers(...)` bindings to include `TileNodeEnergized` (via shared `TileNodeRenderer`) plus the new node-device renderers. This restores non-trivial Stage 8-c visual wiring for energized/stabilizer/converter/relay node progression blocks beyond the earlier core TESR set. Aggregate guard coverage was updated in `ClientProxyTileRendererBurstCoverageTest`; validation: focused test PASS and `validate --smoke` PASS (`compileJava`, `test`, `jar`, `check-jar`, `smoke-server` ready-state pass). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 9-a content delta: restored missing wooden cosmetic block baseline by porting and registering `blockStairsGreatwood`, `blockStairsSilverwood`, `blockSlabWood`, and `blockDoubleSlabWood` (with 1.12 blockstate/model/item-model assets and source-authored `planks_greatwood`/`planks_silverwood` textures copied from `thaumcraft_src/assets`), restoring normal-crafting recipes for greatwood/silverwood stairs+slabs under stable `thaumcraft:*` IDs, and re-adding reference `slabWood` ore-dictionary aliases in `ConfigItems.registerOreDictionary()`. Guard coverage was expanded via new `ConfigRecipesWoodCosmeticRegistryStaticGuardTest` and updated `ConfigItemsOreDictionaryStaticGuardTest`; validation: `compileJava` PASS, focused tests PASS, and `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server` ready-state pass).
Incremental Stage 9-c infusion-instability delta: `TileInfusionMatrix` now restores reference-shaped instability progression for missing ingredients/XP-drain targets by splitting XP drain outcomes (`consumed` vs `targets-without-xp` vs `no-targets`) and using source-matching aspect-backflow chances (`1/(1+a)` per missing ingredient; `1/3` for stalled enchant XP drain with nearby players), and restores reference instability-event ejection signaling via `PacketFXBlockZap` plus flux-goo/gas placement/sound side effects in `inEvEjectItem(...)` without the later replaceable-block gate. Added `TileInfusionMatrixInstabilityStaticGuardTest`; validation: `compileJava` PASS, focused test PASS (`TileInfusionMatrixInstabilityStaticGuardTest`), and `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server` ready-state pass). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c node-TESR fidelity burst delta: upgraded `TileNodeStabilizerRenderer`, `TileNodeConverterRenderer`, and `TileVisRelayRenderer` from billboard-quad fallback rendering to dedicated 3D model-part TESR paths by adding new model classes `ModelNodeStabilizer` and `ModelVisRelay` and wiring reference-shaped transforms/pulses (stabilizer/converter lock+piston geometry with progress-driven extension and overlay tint states, plus vis-relay orientation transforms, ring/base geometry, attunement crystal tinting, and parent-valid glow response via `VisNetHandler.isNodeValid(tile.getParent())`). Validation: `compileJava` PASS, focused renderer coverage test PASS (`ClientProxyTileRendererBurstCoverageTest`), and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server` ready-state pass). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-e FX packet de-dup delta: `PacketFXShield`, `PacketFXSonic`, `PacketFXWispZap`, and `PacketFXZap` now restore single-route reference-shaped emission contracts by removing duplicate fallback overlays (`shieldRunesFX`/`sonicFX`/`burst`, duplicate bolt layering, and extra `playEvent(2005)` spark events) and keeping one explicit FX route per packet branch (`FXShieldRunes`, `FXSonic`, proxy `bolt`, or dedicated `FXLightningBolt` as appropriate). Aggregate guard coverage in `ClientProxyFxStaticGuardTest` now enforces anti-dup routing for these packet handlers. Validation: `compileJava` PASS, focused packet/FX tests PASS (`ClientProxyFxStaticGuardTest`, `PacketFXSerializationTest`), and `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server` ready-state pass). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 9-b arcane-container craft-path delta: restored reference-shaped Arcane Workbench interaction/crafting slot behavior across both container click routing and output-slot consumption hooks by (1) fixing `ContainerArcaneWorkbench.slotClick(...)` to force legacy throw-click routing (`ClickType.THROW` uses drag type `1`) and clamp positive drag-type values on slots `0/1`, replacing the prior incorrect swap-only rewrite, and (2) upgrading `SlotCraftingArcaneWorkbench` to restore crafted-amount tracking/onCrafting callbacks plus Forge container-item remainder handling (`ForgeHooks.getContainerItem(...)`) for 3x3 matrix consumption parity. Added behavioral coverage in `ContainerArcaneWorkbenchClickNormalizationTest` (non-static helper-path assertions) while keeping existing arcane-flow guard coverage. Validation: `compileJava` PASS, focused container tests PASS (`ContainerArcaneWorkbenchClickNormalizationTest`, `ContainerArcaneWorkbenchArcaneFlowStaticGuardTest`, plus container package test run), `validate` PASS, and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server` ready-state pass). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-b/9-b arcane-workbench GUI parity delta: `GuiArcaneWorkbench` is now upgraded from texture-only stub to a reference-shaped arcane-crafting overlay surface by wiring matcher-driven vis cost rendering (`ThaumcraftCraftingManager.findMatchingArcaneRecipe*`), primal-aspect icon+cost tags with wand consumption modifiers, and insufficient-vis ghost result/warning presentation (`Insufficient vis`) when the slotted wand cannot satisfy the current arcane recipe probe. Added aggregate guard coverage in `GuiArcaneWorkbenchStaticGuardTest`; validation: `compileJava` PASS, focused GUI/container tests PASS (`GuiArcaneWorkbenchStaticGuardTest`, `ClientProxyGuiRoutingStaticGuardTest`, `ContainerArcaneWorkbenchArcaneFlowStaticGuardTest`), `validate` PASS, and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server` ready-state pass). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 9-c/9-e infusion-research coverage hardening delta: added aggregate cross-slice audit coverage in `ConfigRecipesInfusionResearchCoverageStaticGuardTest` to enforce reference-sized infusion registration baselines (`63` infusion + `24` infusion-enchantment keys), enforce research-page wiring for `recipeInfusion(...)` / `recipeInfusionEnchantment(...)` against registered keys, and fail on dangling research-page references. The only explicit allowlisted non-page key is `RunicGirdleKinetic_2` (alternate catalyst variant of the same progression recipe branch). Validation: `compileJava` PASS, focused coverage test PASS (`ConfigRecipesInfusionResearchCoverageStaticGuardTest`), and checkpoint `validate` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`); runtime smoke `SKIPPED` for this checkpoint because it is test-only/non-runtime-risk under quota-saving bundle policy.
Incremental Stage 9-b matcher-behavior coverage delta: added `ArcaneRecipeMatcherBehaviorTest` to pin non-static matcher semantics for `ShapedArcaneRecipe`/`ShapelessArcaneRecipe` (base+mirrored shaped matching, mirror-disable rejection, shapeless order independence, and shapeless extra-input rejection) using a test-local `ThaumcraftApi.internalMethods` row/column bridge and `Bootstrap.register()` bootstrap to avoid false failures from dummy API wiring. Validation: focused matcher tests PASS (`ArcaneRecipeMatcherBehaviorTest`, `ThaumcraftCraftingManagerArcaneMatcherStaticGuardTest`) and checkpoint `validate` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`); runtime smoke `SKIPPED` for this checkpoint because it is test-only/non-runtime-risk under quota-saving bundle policy.
Incremental Stage 9-b arcane-wand matcher parity delta: restored strict tag-equality semantics in `ArcaneWandRecipe.checkItemEquals(...)` to match the 1.7.10 reference (`ItemStack.areItemStackTagsEqual` when target stack has NBT), replacing subset-style crafting tag comparison that could incorrectly accept extra input NBT keys. Added `ArcaneWandRecipeTagEqualityBehaviorTest` (reflection-based private matcher probe with bootstrap) to lock exact-match vs extra-tag rejection behavior. Validation: focused arcane matcher tests PASS (`ArcaneWandRecipeTagEqualityBehaviorTest`, `ArcaneRecipeMatcherBehaviorTest`, `ThaumcraftCraftingManagerArcaneMatcherStaticGuardTest`), checkpoint `validate` PASS, and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`).
Incremental Stage 8-c bore-TESR fidelity burst delta: replaced the previous lightweight `TileArcaneBoreRenderer`/`TileArcaneBoreBaseRenderer` fallback visuals with model-driven render paths based on original TC4 bore assets by adding dedicated model classes (`ModelBore`, `ModelBoreBase`, `ModelBoreEmit`, `ModelJar`) and wiring `textures/models/Bore.png` + `jar.png` + layered vortex quads into the bore pipeline. `TileArcaneBoreRenderer` now renders bore chassis/nozzle/emitter parts with orientation-aware transforms and powered-spin behavior (`topRotation` + focus/pickaxe speed), plus reference-shaped multi-layer vortex and jar-core glass overlay; `TileArcaneBoreBaseRenderer` now renders the full base model and orientation-driven nozzle geometry instead of the prior vortex-only indicator. Validation: `compileJava` PASS, focused Stage 8-c renderer binding test PASS (`ClientProxyTileRendererBurstCoverageTest`), checkpoint `validate` PASS, and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c device-TESR fidelity burst delta: upgraded remaining simplified workbench/tube/centrifuge tile renderers to model-driven paths using reference geometry by adding `ModelArcaneWorkbench`, `ModelCentrifuge`, and `ModelTubeValve`, then rewriting `TileArcaneWorkbenchRenderer`, `TileCentrifugeRenderer`, `TileTubeBufferRenderer`, `TileTubeValveRenderer`, and `TileTubeOnewayRenderer` to use `worktable.png`/`centrifuge.png`/`valve.png` model rendering with orientation-aware transforms and state tinting (buffer choke severity, valve flow state, one-way tube stack), replacing prior flat billboard-quad overlays. Validation: `compileJava` PASS, focused Stage 8-c renderer binding test PASS (`ClientProxyTileRendererBurstCoverageTest`), checkpoint `validate` PASS, and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-e infusion-source FX state delta: restored reference-shaped `PacketFXInfusionSource` lifecycle from instant packet-local beam spawn to matrix-owned client FX state by adding `TileInfusionMatrix.sourceFX` (`SourceFX` entries keyed by source vector/color), switching `PacketFXInfusionSource` client handler to scheduled `sourceFX` updates (`15/60` tick windows depending on source tile), and reintroducing client-side `TileInfusionMatrix.doEffects()` flow for startup/crafting sound cadence plus per-tick source-stream and instability bolt emission via proxy FX paths. Aggregate FX guard coverage was updated in `ClientProxyFxStaticGuardTest` for the new packet contract. Validation: `compileJava` PASS, focused FX tests PASS (`ClientProxyFxStaticGuardTest`, `PacketFXSerializationTest`, `TileInfusionMatrixInstabilityStaticGuardTest`), checkpoint `validate` PASS, and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 9-c matcher-guard delta: added `ThaumcraftCraftingManagerInfusionMatcherStaticGuardTest` to pin `ThaumcraftCraftingManager` infusion matcher contracts (`findMatchingInfusionRecipe` and `findMatchingInfusionEnchantmentRecipe`) for null-safe early returns and reference-shaped recipe-list iteration/filter/match routing through `ThaumcraftApi.getCraftingRecipes()`. Validation: focused matcher guards PASS (`ThaumcraftCraftingManagerInfusionMatcherStaticGuardTest`, `ThaumcraftCraftingManagerArcaneMatcherStaticGuardTest`) and checkpoint `validate` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`); runtime smoke `SKIPPED` for this checkpoint because the delta is test/doc-only and non-runtime-risk under quota-saving policy.
Incremental Stage 8-c eldritch-TESR fidelity burst delta: upgraded eldritch portal/lock/obelisk renderer behavior from simplified billboard/overlay fallback paths to more reference-shaped spatial TESR flows by (1) rewriting `TileEldritchPortalRenderer` to use camera-vector billboard quad construction with animated atlas frame slicing and `opencount`-driven dual-axis scale progression, (2) rewriting `TileEldritchLockRenderer` ring stack rendering to textured `ModelCube` geometry with facing-axis rotation semantics plus layered directional field planes (tunnel+particle blend stack with distance fallback), and (3) upgrading `TileEldritchObeliskRenderer` side-field rendering to layered per-face tunnel/particle planes (distance-gated high-detail stack and fallback mode) while preserving dimension-variant side/cap textures and obelisk bob. Validation: `compileJava` PASS, focused Stage 8-c renderer binding test PASS (`ClientProxyTileRendererBurstCoverageTest`), checkpoint `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-e dedicated beam/bolt renderer fidelity delta: replaced simplified fallback particle loops with dedicated custom-render particle implementations by rewriting `FXBeam` to a textured beam `Particle` (`beam.png`/`beam1..3.png` plus impact quad, orientation/end-point updates, `type`/`reverse`/`pulse` controls) and rewriting `FXLightningBolt` to a jittered-segment textured render path (`p_large`/`p_small` layered passes), with `FXBeamGolemBoss` now updating dynamic source/target vectors each tick via `updateBeam(...)` before `super.onUpdate()`. Guard coverage was aligned in `ClientProxyDedicatedBeamBoltStaticGuardTest` for the new jitter-seed/path markers. Validation: focused FX tests PASS (`ClientProxyFxStaticGuardTest`, `ClientProxyDedicatedBeamBoltStaticGuardTest`, `PacketFXSerializationTest`), checkpoint `validate` PASS, and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c jar-renderer fidelity delta: restored missing jar shell/item visual parity by upgrading `TileJarRenderer` to render full jar geometry (`ModelJar` core+lid, void-jar texture branch, node animate scale pulse) instead of contents-only overlays, by aligning brain-jar brine/rotation flow closer to reference (`brain2` + `jarbrine` layered render), and by adding dedicated item-side jar rendering (`ItemJarRenderer` TEISR) wired from `ClientProxy.setupItemRenderers()` via `Item.getItemFromBlock(ConfigBlocks.blockJar)` plus `models/item/blockjar.json` (`builtin/entity`) to route all jar item metas through the renderer. Validation: `compileJava` PASS, focused client tests PASS (`ClientProxyTileRendererBurstCoverageTest`, `ClientProxyDedicatedBeamBoltStaticGuardTest`, `ClientProxyFxStaticGuardTest`), and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c node-item renderer fidelity delta: restored dedicated item-side node visual routing by adding `ItemNodeRenderer` (meta-gated `blockAiry` node/energized-node render path using reference-shaped multi-pass `TileNodeRenderer.renderNodeAt(...)` billboarding) and wiring `ClientProxy.setupTileLinkedItemRenderers()` to bind TEISR for both `blockJar` and `blockAiry`. Added `models/item/blockairy.json` (`builtin/entity`) for runtime item-render dispatch and promoted `TileNodeRenderer.renderNodeAt(...)` to `public static` for shared tile/item usage without duplicate node render logic. Aggregate coverage in `ClientProxyTileRendererBurstCoverageTest` now asserts tile-linked item-renderer wiring. Validation: `compileJava` PASS, focused client tests PASS (`ClientProxyTileRendererBurstCoverageTest`, `ClientProxyDedicatedBeamBoltStaticGuardTest`, `ClientProxyFxStaticGuardTest`), and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-e particle-fidelity delta: restored reference-shaped specialized particle routing for taint/infusion paths by adding missing `FXBoreSparkle` and `FXBreaking`, switching `ClientProxy.drawInfusionParticles3/4` from `FXWispArcing` fallback to tinted `FXBoreSparkle`, and switching `ClientProxy.taintLandFX` / `slimeJumpFX` from sparkle/smoke fallback to colored `FXBreaking` item-crack particles (`Items.SNOWBALL`) with reference-style lifetime shaping. Aggregate FX guard coverage in `ClientProxyFxStaticGuardTest` now asserts the dedicated class usage and class-surface baselines for the new particles. Validation: `compileJava` PASS; focused client tests PASS (`ClientProxyFxStaticGuardTest`, `ClientProxyDedicatedBeamBoltStaticGuardTest`, `ClientProxyTileRendererBurstCoverageTest`); runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-e taint-swarm dedicated FX burst: restored reference-shaped taint swarm-family particle routing by adding missing `FXSwarm` and wiring new proxy FX surfaces `swarmParticleFX(...)` / `splooshFX(...)` (`CommonProxy` no-op, `ClientProxy` client-only implementation), then switching `EntityTaintSwarm`, `EntityTaintSpore`, and `EntityTaintSporeSwarmer` client particle loops from vanilla `SPELL_MOB/SLIME` fallback emissions to dedicated proxy-driven swarm/sploosh paths. Aggregate guard coverage was expanded in `ClientProxyFxStaticGuardTest` and `ClientProxyEntityRendererRegistrationStaticGuardTest` to lock dedicated swarm/sploosh routing contracts. Validation: `compileJava` PASS, focused FX tests PASS (`ClientProxyFxStaticGuardTest`, `ClientProxyDedicatedBeamBoltStaticGuardTest`), runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c focal-manipulator TESR fidelity burst: restored the missing reference `wandtable` model path in `TileFocalManipulatorRenderer` by rendering `ModelArcaneWorkbench` with `textures/models/wandtable.png` and limiting the floating item path to `ItemFocusBasic` stacks (reference-shaped focus-only display contract), replacing the prior model-less aspect-orbit fallback. Added aggregate renderer contract coverage in `TileFocalManipulatorRendererStaticGuardTest`. Validation: `compileJava` PASS, focused renderer tests PASS (`TileFocalManipulatorRendererStaticGuardTest`, `ClientProxyTileRendererBurstCoverageTest`), and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-c wand-pedestal TESR+tile-state burst: restored reference-shaped `TileWandPedestal` drain-state lifecycle by porting node-scan/tick drain flow (`INode` scan cache, 5-tick vis drain attempts for wand/vis-amulet payloads, primal reduction branch gated by `blockStoneDevice` meta `8`, and client drain-vector/color state fields), and aligned `TileWandPedestalRenderer` from prior `focus` overlay fallback to reference-like pedestal item bob/rotation plus additive drain-line rendering from pedestal to active node endpoint (`TileRenderHelper.drawAdditiveLine(...)`). Added aggregate contract coverage in `TileWandPedestalDrainContractsStaticGuardTest`. Validation: `compileJava` PASS, focused renderer/tile tests PASS (`TileWandPedestalDrainContractsStaticGuardTest`, `ClientProxyTileRendererBurstCoverageTest`), and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.
Incremental Stage 8-e client event-layer burst: restored non-stub warp/fog/overlay runtime handling by implementing `ClientTickEventsFML.playerTick(...)` contracts for `warpVignette` brightness control and fog-target decay, and by implementing `RenderEventHandler` overlay/fog hooks (`targetBrightness` + vignette texture pass on `ElementType.PORTAL`, fog density application via `RenderFogEvent`, and player-scoped scan-expiry ticking). Added aggregate contract coverage in `ClientFxEventLayerStaticGuardTest`. Validation: `compileJava` PASS, focused FX/event tests PASS (`ClientFxEventLayerStaticGuardTest`, `ClientProxyFxStaticGuardTest`), and runtime-boundary `validate --smoke` PASS (`git-status`, `compileJava`, `test`, `jar`, `check-jar`, `smoke-server`). GUI/manual visual checks remain `SKIPPED by user instruction: GUI/graphics/user-interactive validation excluded`.

## Archive

- Full checkpoint index: `docs/GOAL_PROGRESS.archive.md`

## Next Checkpoint Candidate

- Continue Stage 8-c/8-e subsystem implementation bursts (real TESR and dedicated FX parity) where runtime-safe non-GUI validation is available.
- After the next Stage 8 burst boundary, move to grouped Stage 9 recipe/content system bursts (not per-line parity micro-commits).
- Keep Stage 7 runtime evidence and any interactive client checks explicitly marked open.

Do not mark Stage 6 or Stage 7 complete from this checkpoint alone.
