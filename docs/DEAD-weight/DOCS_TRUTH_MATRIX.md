# DOCS_TRUTH_MATRIX.md

## Purpose

This truth matrix maps prominent documentation claims to current backend/common/server-side code reality. It is intended as a quick reference while editing `GOAL_PROGRESS.md`, `GOAL.md`, and Stage docs.

Status values:

- `accurate`
- `partially true`
- `optimistic`
- `stale`
- `incorrect`

| File | Claim | Reality in code | Status | Recommended doc wording |
|---|---|---|---|---|
| `AGENTS.md` | Do not claim parity based on compile success alone. | Correct. Current tests include many source-shape/static guards and only some runtime integration tests. | accurate | Keep as source-of-truth rule. Add similar wording to `GOAL_PROGRESS.md`. |
| `AGENTS.md` | Fresh worlds are target; old 1.7.10/WIP saves and external player-data imports are out of scope. | Code uses Forge capability player data and current-world cache; legacy import is not implemented as a main path. | accurate | Keep. Do not introduce save-import expectations in progress docs. |
| `docs/GOAL_PROGRESS.md` | Stage 9-a..e recipe/content/crafting/research systems are substantially ported and guarded. | Large corpus and production code exist, but research/progression and several runtime systems are not server-authoritative or runtime-validated. | optimistic | “Stage 9 has substantial corpus/implementation and many static guards; runtime validation and server-authoritative progression remain blockers.” |
| `docs/GOAL_PROGRESS.md` | `validate` and `validate --smoke` passed. | May be true for last checkpoint, but it proves load/build stability only. It does not prove gameplay parity. | partially true | “Latest documented validation passed for load/build stability; not parity evidence for progression/crafting/worldgen scenarios.” |
| `docs/GOAL_PROGRESS.md` | Client bootstrap, GUI routing, and side boundaries are established. | Broad proxy/GUI routing exists, but Thaumonomicon has no server container and research progression depends on packets not proven wired through GUI. | partially true | “Proxy/GUI routing broadly exists; research progression GUI route remains backend-relevant and unvalidated.” |
| `docs/GOAL_PROGRESS.md` | Stage 8 renderer/FX progress means broad readiness. | Mostly visual/client work; useful for client stability but low backend proof. | optimistic | “Stage 8 visual progress is archived separately and is not backend parity evidence.” |
| `docs/GOAL.md` | Final goal is original TC4 behavior on Forge 1.12.2, preserving API, registry, NBT, research/crafting/worldgen behavior. | Correct as target. Not current state. | accurate as target | Keep, but add a “current backend status caveat.” |
| `docs/GOAL.md` | Stage 9 should implement recipe/crafting/research parity. | Correct target, but wording lacks validation boundary. | partially true | “Populate and runtime-validate recipe/crafting/research behavior; static corpus does not close parity.” |
| `docs/Stage9-a.md` | `ConfigRecipes.init()` is a stub. | False now. `ConfigRecipes.init()` calls arcane/infusion/crucible/smelting setup and populates research recipe handles. | stale / incorrect | “Recipe foundation exists; runtime craftability and parity audit remain open.” |
| `docs/Stage9-a.md` | No recipe registry event for custom recipes. | False now. `Thaumcraft.registerRecipes(...)` calls `ConfigRecipes.registerSpecialRecipes(...)`; `ConfigRecipesSpecialSlice` registers custom/special recipes. | stale / incorrect | “Special recipe registry path exists; validate registry names, duplicates, and craftability.” |
| `docs/Stage9-a.md` | Smelting and smelting bonus are absent. | False now. `ConfigRecipesSmeltingSlice` registers smelting baseline and smelting bonuses. | stale | “Smelting baseline exists; validate behavior and bonus semantics.” |
| `docs/Stage9-a.md` | Lack of JSON recipes is a blocker. | Not necessarily. Current strategy uses Java `IRecipe` registration for many reference-shaped recipes. | optimistic / partially true | “JSON absence is not itself a blocker when Java registration is intentional and audited.” |
| `docs/Stage9-a.md` | Object/aspect tag coverage far below reference. | Coverage has expanded; full scan/content parity still requires validation. | stale / partially true | “Aspect tag corpus is expanded and guarded; scan/object tag behavior remains runtime-unverified.” |
| `docs/Stage9-d.md` | API/runtime baseline exists but content data is not registered. | Contradictory/stale. Current crucible slice registers a broad recipe corpus; smelting bonus baseline exists. | stale | “Content corpus is present; runtime alchemy/thaumatorium validation remains the closure gate.” |
| `docs/Stage9-d.md` | Crucible/alchemy recipe registration is absent. | False/stale. `ConfigRecipesCrucibleSlice.initializeCrucibleRecipeBaseline()` registers many crucible recipes. | stale / incorrect | “Crucible corpus present; parity audit and runtime validation open.” |
| `docs/Stage9-d.md` | Crucible and thaumatorium scenarios are not runtime-verified. | Correct. Production code exists, but scenario validation is still needed. | accurate | Keep and strengthen as the primary Stage 9-d closure gate. |
| `docs/Stage9-d.md` | `TileThaumatorium.completeRecipe()` clears stored essentia; needs validation. | Correct as a risk/verification point. Not proven bug. | accurate | “Reference-shape or intended behavior needs exact/extra essentia runtime verification.” |
| `docs/Stage9-e.md` | Research categories and entries are not registered. | False/stale. Six categories and 201 `ResearchItem` registrations exist. | stale / incorrect | “Research corpus exists; graph correctness and runtime progression validation remain open.” |
| `docs/Stage9-e.md` | Research localization is absent. | Stale. Lang corpus is present enough for static coverage claims. | stale | “Localization corpus present; live page rendering not validated.” |
| `docs/Stage9-e.md` | Research assets are missing. | Stale/partially true. Baseline GUI/misc research assets exist; full visual/image-path validation remains open. | stale / partially true | “Baseline assets present; visual/manual asset completeness remains separate from backend closure.” |
| `docs/Stage9-e.md` | Research note/data flow is missing; no `ResearchNoteData`. | False now. `ResearchNoteData`, note NBT, creation/update/completion methods exist. | stale / incorrect | “Research note model exists; end-to-end note gameplay remains unvalidated.” |
| `docs/Stage9-e.md` | Hidden/lost research discovery from scans is not wired. | False now. `ResearchManager.createClue(...)` exists and `ScanManager.completeScan(...)` forwards clue data. | stale | “Scan clue wiring exists; prerequisite/runtime behavior needs verification.” |
| `docs/Stage9-e.md` | Research completion packet bypasses original progression rules. | Stale as written. `PacketPlayerCompleteToServer` has meaningful server checks now. | stale / partially true | “Completion packet has server checks; live GUI route and adversarial tests remain open.” |
| `docs/Stage9-e.md` | Research/progression is complete enough. | Not true. Research table packets and scan packet lack server-authoritative validation; GUI route not proven. | optimistic / incorrect if closure claim | “Research data/model is advanced; progression runtime is not complete.” |
| `docs/Stage4.md` | Thaumatorium top tile is a stub; containers are empty shells. | Historical/stale in early gap list. Later closure notes and code show many implementations. | stale unless read as history | “Historical gap; current code has implementations. Remaining risk is runtime validation and semantic parity.” |
| `docs/Stage4.md` | Infusion Matrix and Crucible need runtime validation. | Still correct. | accurate | Keep. Tie to Stage 9 content validation. |
| `docs/Stage6.md` | Stage 6 should not be marked complete without runtime/manual entity scenarios. | Correct. Many static checkpoints exist but entity AI/combat requires live validation. | accurate | Keep and surface in `GOAL_PROGRESS.md`. |
| `docs/Stage7.md` | Outer Lands baseline restored. | Partially true only if referring to scaffold/hook/ring/maze code. Portal/maze runtime validation remains open. | optimistic if used as closure | “Outer Lands scaffold and hooks are present; ring/maze/portal/safe-spawn/persistence runtime validation remains open.” |
| `docs/Stage7.md` | Empty `ChunkProviderOuter` generation means no Outer Lands generation. | Too simplistic. `ThaumcraftWorldGenerator.worldGeneration(...)` calls `MazeHandler.generateEldritch(...)` for the Outer dimension. | incorrect if stated broadly | “Chunk primer is intentionally empty; maze generation is delegated to worldgen hook. Runtime proof remains open.” |
| `docs/Stage7.md` | Portal teleporter unsafe / portal behavior not complete. | Still supported by code: `TeleporterThaumcraft.makePortal(...)` returns true without creating a portal; safe destination behavior needs validation. | accurate | Keep, but include current search/cache/fallback behavior accurately. |
| `docs/PRD.md` | Only validated systems should be treated as closed. | Correct and important. | accurate | Keep. Add progress vocabulary if desired. |
| Tests / progress docs | Static guard tests prove parity. | False. They protect source/corpus shape, not live behavior. | incorrect | “Static guarded only; runtime validation still required.” |
| Tests / progress docs | Packet count guard proves network parity. | False. `PacketHandler.REFERENCE_PACKET_COUNT = 39` guards discriminator count, not C2S authorization semantics. | incorrect | “Packet IDs guarded; packet behavior and malicious payload rejection remain separate validation.” |
| Code/progress | Arcane crafting is one of the stronger backend systems. | Supported. Arcane recipe classes, container/slot flow, dynamic wand/sceptre recipes, and integration tests exist. | accurate | “Arcane crafting is materially implemented and relatively well-tested, with remaining edge-case validation.” |
| Code/progress | Backend substantially complete. | Not supported. Several core systems are not runtime-validated or server-authoritative. | incorrect | “Backend implementation is substantially populated; backend parity is not substantially validated.” |
